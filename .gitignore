# dependencies / build
node_modules
.next
out
*.tsbuildinfo
next-env.d.ts

# generated
src/generated

# local database
prisma/dev.db
prisma/dev.db-journal
prisma/*.db
prisma/*.db-*

# secrets
.env
.env.*
!.env.example
!.env.mac.example
!.env.example
google-service-account.json
*.service-account.json

# customer / company reference data — shared out of band, never committed
data/reference/
!data/fixtures/
!data/fixtures/**
samples/
*.xlsx
*.xls
!docs/**/*.xlsx

# editor / os / misc
*.log
# the command logs of a debug run are its evidence (TEST_RESULTS.md cites them by name)
!docs/debug-runs/**/*.log
.DS_Store
Thumbs.db
.vscode/
.idea/
__pycache__/
*.py[cod]
*.zip
docs/crosswalk-build-notes.md

# course files that happen to live in this folder
*.py

# one-time bootstrap artefacts
crosswalk.bundle
*.bundle
push-to-github.ps1
data/fixtures/integrations/pushed-quotes/
.env.debug
.env.local.pg

# benchmark cases are customer lists
data/benchmark/*
!data/benchmark/README.md

# Delivery bundles from Claude sessions and the folder used in place of deletes
.claude-bundles/
_to_delete/
scratch/

# uploaded document bytes (DOCUMENT_STORAGE_DIR)
.data/
.scratch/
