# Blockers, prerequisites and decisions

Each item names what is missing, what was tried, what can and cannot be proven, and the smallest next action.

| # | Item | Missing | Tried / what is proven | Cannot be proven here | Smallest next action |
|---|---|---|---|---|---|
| B-01 | `eval:gate` (CW-DBG-0003 / WS1-F26) | a model key and an accept decision | gate fails deterministically: "BIN_VERSION is 8 but the accepted baseline measured version 6; LLM_MODEL gpt-5.6-astra vs gpt-6-astra"; the gate was not weakened; heuristic eval improved 26→29/80 top-1 with every moved line explained (WS1 §K) | model grading agreement at bins v8 | `OPENAI_API_KEY=… npm run eval:model` → review the report → `npm run eval:model -- --accept` → commit `data/eval/model-baseline.json`; CI on `main` stays red at this step until then |
| B-02 | Docker image build and boot (CI `image` job, WS5-F08 verification) | a Docker daemon | `deploy/entrypoint.sh` roles exercised directly (check/migrate/worker, SIGTERM → `jobs.shutdown`, exit 0); Dockerfile reviewed statically | that the image builds and the container boots | on a Docker host: `docker build -t crosswalk:ci .` then the `image` job steps of `.github/workflows/ci.yml`; `docker stop web` must log `jobs.shutdown` |
| B-03 | Live providers: Salesforce, SAP, GPO rosters, OCR vendor, ECB/FX HTTP, AvaTax, SAM.gov, USAspending, SMTP, Teams, Google Drive, IdP | credentials / sandboxes | every family passes a 9-scenario fault matrix and mapping/idempotency/paging tests against the labelled mocks and injected fetch; OIDC against an in-memory RS256 IdP; AvaTax uncommitted SalesOrder against a fake | wire compatibility with the real services | company supplies credentials per `docs/INTEGRATION_SETUP.md`; run Settings → Integrations → Test / Validate against provider / Sync a test record; register the IdP and sign in once |
| B-04 | macOS / `mac-demo` execution | a Mac | scripts read-only reviewed (KN-24): localhost guard is a substring match (weak), `start.sh` falls back one port only, `seed-demo.ts` predates `OWN_LABELERS` | the scripts run on macOS | run `npm run mac:setup -- --docker`, `mac:doctor`, `mac:start` on a Mac after merging `main` into `mac-demo`; tighten the host check as noted in WS5 §5 |
| B-05 | Browser matrix beyond Chromium | ~~Firefox/WebKit engines~~ **DONE 2026-09-24 23:00** (TEST_RESULTS §7): Playwright Firefox 142 and WebKit 26 installed here; matrix 264/264, responsive-axe 0 violations / 0 of 88, keyboard 18/18, navigation 26/26 and the proposal journey pass on both; the request journey passes on Firefox and reaches 42/47 on WebKit before the Linux WebKit engine itself aborts (CW-DBG-0010) | real Safari (macOS) | rerun `PW_BROWSER=webkit node evidence/ws3/journey-request.js` in Safari on a Mac (B-04) |
| B-06 | Published crosswalk versions made by the old `publishVersion` (WS1-F10) | a re-publish per environment | on this run's DBs no published entry carries a note-SKU; the Neon dev DB was never touched | that Neon's published version is clean | on Neon after deploy: `SELECT "ownSku", count(*) FROM "CrosswalkVersionEntry" WHERE "ownSku" IN ('DUPLICATE','REPEAT','HAND','DISCONT','ALTERNATE','WRONG','TAKE','THIS') GROUP BY 1;` — if any rows, Crosswalk → Publish new version |
| B-07 | B12LTH → NONB12STF vs the curated "Exact" ONB12STF (REV-13 / CW-DBG-0002) | sheet-owner confirmation | GUDID text "ENDOPATH XCEL Bladeless Trocars 12 mm; 100 mm" carries no OPTIVIEW/optical marker; the SKU convention and brand rule read it as non-optical; three curated sheets cross it to the optical VersaOne; the code follows MATCH_QUALITY_MODEL §4/§5 (optical difference = Close cap) | whether the curated sheets encode product knowledge the text lacks | sheet owner confirms; if B12LTH is optical, add a competitor-size/spec row saying so (curated-spec has top priority) and the curated Exact stands again |
| B-08 | Scope rule: child account of an unassigned parent visible to every rep (WS4-F25) | a business decision | rule as documented in BUILD_NOTES §7.4 composes into this; probes use stand-alone accounts | which behaviour is wanted | decide; if the child's own owner/territory should win, change `accountWhere` (`{ parent: {…}, ownerUserId: null, territory: null }`) and add a scope test |
| B-09 | Thoracoport component (WS1-F27) and 14 inconsistent curated rows (WS1-F32, REQ-7628 report §8) | sheet owner | classified with evidence; nothing rewritten | product truth | sheet owner retires/corrects rows; re-run `npm run eval` |
| B-10 | Historical benchmark lists (`npm run benchmark`) and the PACR export (`seed-sanford-benchmark.ts`) | company-owned files | harness code paths covered by unit/db tests | accuracy on historical lists | drop lists into `data/benchmark/`; rerun `eval-pacr.ts run` with the export |
| B-11 | 5,000-line performance profile and a timed soak | — | **DONE 2026-09-24 23:33** (TEST_RESULTS §7): 5,000 distinct codes from a bulk-imported labeler (library-first, 0 per-code openFDA calls): cold 111 s / warm 25.7 s, complete; 30-min soak of 24 × 300-line runs with a web SIGTERM restart and a Postgres restart, all complete and consistent; a 5,000-line run SIGTERM'd mid-match resumed from its checkpoint to complete with no duplicates | longer soaks / multi-day | optional: batch the per-line resolution writes (CW-DBG-0011) |
| B-12 | Push / deploy / Neon migrations | Alex's credentials (by design) | nothing pushed; no migration added by this run (schema comments only) | — | `git push origin debug/2026-09-24-full-application` (or merge to `main`), `npx prisma migrate deploy` is a no-op for this run, `npx prisma generate`; set `SSO_PROXY_SHARED_SECRET` if `SSO_MODE=proxy`; `OWN_LABELERS` if not Covidien/Medtronic/Sofradim |
