# WS1 report — matching, intake, GUDID, catalog, evaluation, learning loop

Branch `debug/2026-09-24-full-application`, env `.env.local.ws1`, DB `crosswalk_ws1` (copy of `crosswalk_dbg`), port 3101 (not needed: no browser tests in this workstream). No commits, no pushes, nothing against Neon. Evidence: `docs/debug-runs/2026-09-24-full-application/evidence/ws1/` (referred to as `EV/` below; `EV/scripts/` holds the probe scripts that produced the `*-probe.txt` and `b12lth-evidence*.txt` files).

## 1. Inspected scope

Read in full: `docs/BUILD_NOTES.md` §9–12, §23, §26; `docs/MATCH_QUALITY_MODEL.md`; `docs/BUSINESS_RULES.md`; `src/lib/match/{access,bin,brands,component,constraints,grading,line,score,embeddings}.ts`; `src/lib/pipeline/{run,resolve}.ts`; `src/lib/gudid/{http,openfda,library,library-model,refresh,enrich}.ts`; `src/lib/excel/intake.ts`, `src/lib/sheets/csv.ts`, `src/lib/excel/sizes.ts` (units only); `src/lib/cfn.ts`; `src/lib/settings.ts`; `src/lib/xref/{learning,governance}.ts`; `src/lib/eval/model.ts`; `scripts/{eval,test-enterprise}.ts`; `prisma/seed-enterprise.ts`, `prisma/seed.ts` (bin construction only); `tests/unit/{match-quality,pacr-regression}.test.ts`, `tests/db/{tier1,tier3}.test.ts` (relevant blocks).

Behaviours exercised: B12LTH ranking (A), tier3 embedding sweep (B), Settings → run weights (C), grade ordering (D), dead code (E), intake matrix (F), access parser / brand rules / constraint boundaries (G), ranking / classification / confidence separation incl. SELF_MATCH and the cap-bound grader (H), run lifecycle incl. cancel, chunked-persist failure, resume, re-run policy, learning loop (I), openFDA client + GUDID library import/adopt/prune (J), evaluation integrity before/after (K), code sweep (L).

Not done: no HTTP/browser tests (routes are WS4, pages WS3); `src/lib/catalog/{cost,fx,size-coverage}.ts` were only read for the sweep (no defects found, no tests added); `src/lib/llm/tasks.ts` model paths beyond the fake grader; embeddings retrieval beyond the existing tier3 coverage; `scripts/eval-pacr.ts` was not re-run (needs the REQ-7628 request in the DB).

## 2. Findings

| id | sev | type | title | reproduction | root cause | fix (files) | regression test | pre-fix evidence | post-fix | status |
|---|---|---|---|---|---|---|---|---|---|---|
| WS1-F01 | P1 | TEST_DEFECT | CW-DBG-0002: `test:enterprise` expects ONB12STF for B12LTH; NONB12STF is the evidence-based pick | `npm run test:enterprise` → 13/18, 5 failed (`EV/enterprise-before.txt`) | B12LTH = Endopath Xcel (non-optical per `ethicon.xcel`/SKU rule `B…LT` → non-optical, bladeless, 12 × 100, handled). NONB12STF agrees on all 5 decisive fields → attribute Exact, conf 0.80. ONB12STF is optical → curated Exact contradicted by the soft "optical vs non-optical" cap → Close (MATCH_QUALITY_MODEL §4/§5); grade orders first. KnownCross rows for B12LTH: ONB12STF Exact (PACR/Sheet1/Nikki-Mike), NB12STF/ONB12LGF/ONB12SHF/176674PF Close; no row for NONB12STF (`EV/b12lth-evidence.txt`, `EV/b12lth-evidence-after.txt`) | `scripts/test-enterprise.ts` (expectation, `by()`, purchase record → NONB12STF); `prisma/seed-enterprise.ts` (NONB12STF added to the demo SKU list with demo list/cost mirroring ONB12STF, labelled as fixtures) | `test:enterprise` step 1 | 5 failed | `EV/enterprise-after.txt`, `EV/enterprise-final.txt`: 18 passed, EXIT 0, process exits on its own in ~7 s | VERIFIED_FIXED |
| WS1-F02 | P1 | DEFECT | Versaport Plus "Bladeless N mm … with fixation cannula" SKUs (NB12STF, NB12LGF, NB12SHF, NB12STS, NB15LGF, NB15STF, NB5LGF) classified **cannula** | `EV/scripts/b12lth.ts` before: NB12STF curated Close for B12LTH ranked **No Match** ("✗ cannula only"); `EV/own-trocar-components-before.txt`; `psql` on `OwnProduct.binJson` shows `component=cannula via brand:mdt.versaport-sleeve` | brand rule `mdt.versaport-sleeve` (`unless: trocar|obturator`) fired on "with fixation cannula"; the generic `componentOf` also read "…with fixation cannula" as sleeve-only; stored bins had no SKU-convention evidence (F04) | `src/lib/match/brands.ts` (`unless` adds `with … cannula/sleeve`), `src/lib/match/component.ts` (`withCannula` recognises tip-word + "with … cannula"), `src/lib/match/bin.ts` BIN_VERSION 7 → 8 | `tests/unit/ws1-access.test.ts` "Versaport: 'Bladeless … with fixation cannula' is a complete trocar…" | `EV/ws1-access-prefix.txt` (5 failed pre-fix) | 30/30 | VERIFIED_FIXED |
| WS1-F03 | P1 | DEFECT | NBFCA12ST "fixation cannula … for use with Versaport Plus Bladeless trocar" classified **trocar** → ranked attribute **Exact** (#2) for trocar lines and No Match for cannula lines | `EV/b12lth-evidence.txt` (NBFCA12ST Exact, conf 0.74); eval line CTB12LT | `withCannula` regex joined "cannula … with … trocar" across "for use with" | `src/lib/match/component.ts` (`(?<!for use )with` and a 4-token window) | `ws1-access.test.ts` (same case); `tests/unit/pacr-regression.test.ts` proxy regex excludes "for use with" (TEST_DEFECT: it only passed because of the mis-classification) | pacr "no trocar is Exact or Close for a cannula-only line" failed after the fix until the proxy was corrected | eval CTB12LT T3 → T1 | VERIFIED_FIXED |
| WS1-F04 | P2 | DATA_QUALITY | Seeded own-product bins were built without `manufacturer`, so SKU conventions never applied (no `sku:` evidence in any stored own bin) | `psql`: `binJson.access.evidence` for NB12STF/NB5STF has no `sku` source; `prisma/seed.ts:104,118,171` call `heuristicBin` without `manufacturer` | seed omits the labeler/company; bins were v7 so the pipeline never rebuilt them | BIN_VERSION bump (F02) forces the pipeline/eval to rebuild with `labeler ?? company.name`; `prisma/seed.ts` is shared — suggested patch: pass `manufacturer: COMPANY` (and `code: sku`) in the three `heuristicBin` calls | covered by ws1-access SKU-convention cases | — | own bins rebuilt on first run (test:enterprise run did) | FIXED_AWAITING_VERIFICATION (seed patch is WS5/shared) |
| WS1-F05 | P2 | DEFECT | Ranking and the eval depend on physical DB row order on exact ties (no deterministic tiebreak): identical code gave top-1 26 → 28 after tier3 rewrote every OwnProduct row | `EV/eval-before.txt` (26/46) vs `EV/eval-all-before.txt` (28/46, same code, after the embedding UPDATEs); `EV/eval-all-before-det.txt` (27/45 with SKU-ordered pool); codes B11LT, CB11LT, TLC10, TLC75 flip by order | `scoreCandidates` sort ended at "has a price"; `attributeShortlist` and the pipeline pool sort had no tiebreak; `scripts/eval.ts` loaded own products without `orderBy` | `src/lib/match/score.ts` (final `sku.localeCompare`), `src/lib/match/line.ts`, `src/lib/pipeline/run.ts` (pool sort), `scripts/eval.ts` (`orderBy: { sku }`) | `tests/unit/ws1-ranking.test.ts` "ties are broken by SKU…" | `EV/ws1-ranking-prefix.txt` | eval 29/46 twice in a row (`EV/eval-after.txt`) | VERIFIED_FIXED |
| WS1-F06 | P2 | TEST_DEFECT | CW-DBG-0001: tier3 "refreshEmbeddings writes vectors once" state-dependent | `UPDATE "OwnProduct" SET embedding=NULL…` then `npx vitest run tests/db/tier3.test.ts -t "refreshEmbeddings writes vectors once"` → `expected 60 to be +0` (`EV/tier3-before.txt`) | first limit-60 sweep leaves the seed backlog pending | `tests/db/tier3.test.ts` (3.5 block: drain loop until scanned==0, then null the two SKUs and assert scanned==2/embedded==2/unchanged==0) | itself | `EV/tier3-before.txt` | `EV/tier3-after.txt`: 17/17 fresh, 17/17 warm | VERIFIED_FIXED |
| WS1-F07 | P1 | DEFECT | KN-06: Settings weights / maxCandidates never reached queued runs | `tests/db/ws1-run.test.ts` KN-06 case pre-fix: `optionsJson` = `{freshGrades:false}`, factors.weights = defaults (`EV/ws1-run-prefix.txt`) | `enqueueRun` wrote optionsJson without weights; `runRequestInner` read `parse.weights ?? {}` whenever optionsJson existed | `src/lib/pipeline/run.ts`: enqueue snapshots `{weights, maxCandidates}` (kept on retry/orphan resume, re-taken on a fresh Re-run); the run reads only the snapshot through `sanitizeWeights` (finite, 0–10, ≥ 1 positive) and a 1–25 maxCandidates guard; policy in `docs/MATCH_QUALITY_MODEL.md` §5.2 | `ws1-run.test.ts` "KN-06 …" (custom price-heavy weights flip B over A, factorsJson carries them, grades/caps unchanged, retry + orphan resume keep the snapshot, fresh Re-run re-snapshots) and "a corrupt or hostile snapshot…" | 4 failed pre-fix | 9/9 (`EV/ws1-run-after.txt`) | VERIFIED_FIXED |
| WS1-F08 | P2 | DEFECT | KN-11: "US Downsell Match" ranked 2 in `score.ts` and 3 in `line.ts` | `MATCH_ORDER` vs `GRADE_ORDER` | two tables | one table `MATCH_ORDER`/`gradeRank` in `score.ts` (Exact > Close > Alternative > Downsell > No Match — a downsell is PARTIAL_SUBSTITUTE with the lower floor 0.60), `line.ts` `betterCross` reads it; documented in `MATCH_QUALITY_MODEL.md` §5.1 | `ws1-ranking.test.ts` "one grade ordering everywhere…" | `EV/ws1-ranking-prefix.txt` | pass | VERIFIED_FIXED |
| WS1-F09 | P3 | DEFECT | KN-14: `cfnVariants()` dead | `grep -rn cfnVariants src scripts tests` → only the definition (the `hints.cfnVariants` matches are a schema field) | superseded by `resolve.ts variantsFor` | removed from `src/lib/cfn.ts`; header comment updated | typecheck | — | `npm run typecheck` EXIT 0 | VERIFIED_FIXED |
| WS1-F10 | P1 | DEFECT + DATA_QUALITY | Reviewer notes in `preferredOwnSku` (DUPLICATE, REPEAT, HAND, DISCONT, WRONG, TAKE, THIS, DOESNT…) were treated as SKUs: `curatedCandidates` dropped those rows from matching, and `publishVersion` published them as own SKUs — published crosswalk v3 holds **654** such entries (412 DUPLICATE, 115 REPEAT, 59 HAND, 57 DISCONT, …) which `approvedCross()` hands to proposals | `psql` on `CrosswalkVersionEntry` join PUBLISHED version (report §3 cmd 12); `ws1-run.test.ts` publish case pre-fix: 654 placeholder entries (`EV/ws1-run-prefix.txt`) | redirect regex `/^[A-Z0-9-]{4,}$/` accepts any word | `src/lib/match/line.ts` (redirect only to a catalog SKU, else `ownSku`), `src/lib/xref/governance.ts` (`publishVersion` redirects only to catalog SKUs and skips placeholder own SKUs; audit records `skippedPlaceholders`); `MATCH_QUALITY_MODEL.md` §5.4 | `ws1-ranking.test.ts` "preferred SKU redirection…", `ws1-run.test.ts` "publishVersion never publishes a reviewer note…" | above | pass; **the live published v3 still carries the bad entries until governance publishes a new version** (my DB: test publishes and restores v3) | FIXED_AWAITING_VERIFICATION (re-publish needed in every environment) |
| WS1-F11 | P2 | DEFECT | Model-grading sibling floor lifted a hard-capped No Match (cannula for a trocar line) to Alternative | `ws1-ranking.test.ts` "the sibling floor never lifts a hard-capped No Match" pre-fix | Pass 2 of `applyGroupGrades` ignored `factors.cap` | `src/lib/match/grading.ts` | same | `EV/ws1-ranking-prefix.txt` | pass | VERIFIED_FIXED |
| WS1-F12 | P3 | DEFECT | SELF_MATCH successor handling was one hop, no cycle guard, no invalid-successor message | code reading (`run.ts` Phase A) | inline logic | `src/lib/match/line.ts` `resolveSelfMatch` (chain ≤ 5 hops, visited set, placeholder check, explanations); `run.ts` loads successor rows round by round (bounded) | `ws1-ranking.test.ts` "SELF_MATCH successors" (active / discontinued+active successor / chain / missing / not in catalog / inactive / invalid / A→B→A / A→A / > 5 hops) | function absent pre-fix | pass | VERIFIED_FIXED |
| WS1-F13 | P2 | DEFECT | Re-run overwrote a reviewed line's manual selection with the new top pick while leaving `reviewed = true` | `ws1-run.test.ts` "re-run keeps a reviewed line's chosen SKU…" pre-fix: selection id changed to the new rank 1 | `persistChunk` always selected rank 1 | `src/lib/pipeline/run.ts` (reviewed lines keep the chosen SKU when still offered — selection follows the SKU, `isSelected` moved; otherwise new top pick + `reviewed` cleared + run-log line); policy `MATCH_QUALITY_MODEL.md` §5.3 | same test (both branches, notes untouched, log line) | `EV/ws1-run-prefix.txt` | pass | VERIFIED_FIXED |
| WS1-F14 | P2 | DEFECT | Intake: header window counted blank rows (header on physical row 11 → "ITEM" ingested as a code); a multi-word title row ("Hospital list") ingested as a code; a repeated page header ("Product Code") ingested as a code | `EV/intake-probe.txt` rows "shifted header row 11", "no header first row junk" | fixed 10-physical-row window; no-header fallback only rejected non-code-looking first cells; no header check inside data | `src/lib/excel/intake.ts` (10 non-empty rows; title heuristic; `CODE_HEADERS` rows inside data → ignored "column header, not a product") | `tests/unit/ws1-intake.test.ts` (2 cases) | `EV/ws1-intake-prefix.txt` (6 failed pre-fix) | 19/19 | VERIFIED_FIXED |
| WS1-F15 | P3 | DEFECT | Intake: a 42-character code was rejected as "does not look like a catalog number" while the documented limit is > 42 | `EV/intake-probe.txt` "code len 42/43" | `looksLikeCfn` allowed 41 chars; reason text said "42+" | `src/lib/cfn.ts` (≤ 42), `intake.ts` reason "more than 42 characters" | `ws1-intake.test.ts` "code length…" | above | pass | VERIFIED_FIXED |
| WS1-F16 | P3 | DEFECT | Intake: a corrupt/empty `.xlsx` surfaced ExcelJS's zip error ("Can't find end of central directory … see https://…") to the rep via the 400 response | `EV/intake-probe.txt` "corrupt xlsx throws" | no wrapping in `parseIntake` | `intake.ts` (`<file> is not a readable .xlsx workbook (empty, corrupt, or a different format …)`) | `ws1-intake.test.ts` "a corrupt or empty .xlsx…" | above | pass | VERIFIED_FIXED |
| WS1-F17 | P3 | DEFECT | Intake: "Est. Competitor Price" / "Avg. Price" headers not detected (the dot) | probe "price header variants": price col null | `PRICE_HEADERS` | `intake.ts` regex | `ws1-intake.test.ts` "price and description headers…" | above | pass | VERIFIED_FIXED |
| WS1-F18 | P3 | DEFECT / SPEC_AMBIGUITY | Intake quantities with a unit ("12 EA") became 1; non-numeric words ("abc", "NaN") also become 1 | probe "qty edge" | `cellNumber` returned null for anything non-numeric | `intake.ts` reads a leading number with a unit. Words → 1 is **kept**: BUILD_NOTES §9.1 lists only ≤ 0 and > 10 M as skips and `scripts/test-adversarial.ts` pins `1DLMC05,abc` → merged quantity 4 (my first version skipped it and broke the adversarial suite — reverted). Recommendation for the owner: a `warnings` channel in the accounting rather than a silent 1 | `ws1-intake.test.ts` "quantity…" | — | `test:adversarial` 24/24 | VERIFIED_FIXED (units) / OPEN (words, spec) |
| WS1-F19 | P2 | DEFECT | Access profile ignored structured sizes not in mm: GUDID "Centimeter" lengths and curated imports (whose unit **defaults to cm**, `excel/sizes.ts:86`) never reached the profile; text "10 cm length" / "12 mm x 10 cm" unread | `EV/access-probe.txt` before: "gudid cm length … l=null" | `buildAccessProfile` skipped non-mm units; parser had no cm | `src/lib/match/access.ts` (`toMm`, cm/inch structured sizes; text lengths in cm for 3–20 cm; "Round 12 cm x 1" pack guard kept) | `ws1-access.test.ts` "cm → mm…" | `EV/ws1-access-prefix.txt` | pass | VERIFIED_FIXED |
| WS1-F20 | P2 | DEFECT | Brand rules and feature tags ignored negations: "non-optical" → optical; "without fixation" → fixation; "non-bladed" also fired `generic.bladed`; `FEATURES` tags likewise (`optical`, `bladed`, `fixation cannula` on negated text) | `EV/access-probe.txt` before | regexes without look-behinds | `src/lib/match/brands.ts` (`generic.non-optical`, `generic.without-fixation`, look-behinds on optical/bladed/fixation), `src/lib/match/bin.ts` FEATURES | `ws1-access.test.ts` (3 cases) | above | pass | VERIFIED_FIXED |
| WS1-F21 | P2 | DEFECT | openFDA client had no request timeout — a hung connection blocked a run forever | code reading; `ws1-gudid.test.ts` "a hung request times out" pre-fix: `timeoutMs is not a function` / would hang | no AbortController | `src/lib/gudid/http.ts` (`OPENFDA_TIMEOUT_MS`, default 20 s; abort → retried like a network failure; metric outcome `timeout`) | `tests/db/ws1-gudid.test.ts` | `EV/ws1-gudid-prefix.txt` | 9/9 (`EV/ws1-gudid-after.txt`) | VERIFIED_FIXED (`.env.example`/BUILD_NOTES §5 mention is WS5's) |
| WS1-F22 | P3 | DEFECT | Token bucket stalls every openFDA caller when the clock steps backwards (`now - last` negative drains the bucket) | reproduced while writing the memo-expiry test with a moved clock: 30 s hang | `refill()` | `http.ts` (`elapsed = max(0, …)`) | indirectly by the memo test; `EV/vitest-full-*.txt` | first ws1-gudid run timed out 4 tests | pass | VERIFIED_FIXED |
| WS1-F23 | P3 | DEFECT | `pruneAdopted` dry-run over-reported already-inactive referenced rows and the apply rewrote them (not idempotent) | `ws1-gudid.test.ts` adoption case: second dry-run `deactivated: 1` | inactive rows re-counted | `src/lib/gudid/library.ts` | same | `EV/ws1-gudid-prefix.txt` | pass | VERIFIED_FIXED |
| WS1-F24 | P3 | DEFECT | Raw `e.message` (Prisma invocation dumps, driver text) written into rep-visible `resolutionNote`, the run log, the enqueue failure and the GUDID import log | code reading (`run.ts` 3 sites, `library.ts` 2 sites) | no sanitising | `run.ts` `publicMsg()` (→ `publicErrorMessage`), `library.ts`; raw text still goes to the structured server log | covered by existing tier1 "never shows raw driver text" + full suite | — | pass | VERIFIED_FIXED |
| WS1-F25 | P3 | DEFECT | A corrupt `gudidJson` on one competitor/own row failed the whole run in the bin stage (`JSON.parse` inside `mapLimit`) | code reading | unguarded parse | `run.ts` `parseRecord()` (warn + bin from description) | — (no fixture; low risk) | — | typecheck/suite pass | FIXED_AWAITING_VERIFICATION |
| WS1-F26 | P2 | PREREQUISITE | `npm run eval:gate` fails at baseline and after: BIN_VERSION 7 (now 8) vs accepted baseline 6; model name mismatch | `EV/eval-gate.txt` EXIT 1 (also failed before my changes with "7 vs 6") | `data/eval/model-baseline.json` was accepted at bins v6; re-measuring needs `OPENAI_API_KEY` | none (baseline must not be edited) | — | — | — | OPEN (needs a key: `npm run eval:model -- --accept`) |
| WS1-F27 | P3 | SPEC_AMBIGUITY | Thoracoport own SKUs 179301/303/305/307 ("… Single Use Trocar; Non-conductive Sleeve") classify as cannula; curated TT012 (Endopath thoracic trocar **sleeves**) expects them, so changing it would regress TT012 | `EV/own-trocar-components-before.txt` | sleeve-terminated description | none | — | — | — | OPEN (sheet owner to confirm whether Thoracoport is sold as a sleeve) |
| WS1-F28 | P3 | TEST_DEFECT | GUDID jobs enqueued by a DB test with `JOBS_WORKER=off` linger in pg-boss and are picked up by another suite's in-process worker (tier1's RUN_COMPLETE notification vanished in the first full run) | `EV/vitest-full-1.txt` (2 tier1 failures), `psql pgboss.job` (7 `gudid.import` created) | my new `ws1-gudid` suite | `tests/db/ws1-gudid.test.ts` cleanup cancels its jobs | full suite ×3 green | — | `EV/vitest-full-2.txt`, `-3`, `-final`: 388/388 | VERIFIED_FIXED |
| WS1-F29 | P3 | NOT_A_DEFECT | BUILD_NOTES §23.2/§26.2 say `test:enterprise` / `test:adversarial` do not exit on their own | observed: both exit with code 0 on their own under `timeout 600` (test:enterprise in ≈ 7 s wall time; `EV/enterprise-final.txt`, `EV/adversarial-final.txt`) | stale note | doc note is WS5's | — | — | — | NOT_A_DEFECT_WITH_RATIONALE |
| WS1-F30 | P3 | SPEC_AMBIGUITY | Corrupt `optionsJson` on a request: I first made the run tolerant (fall back to Settings); WS5's `tests/db/tier1.test.ts` uses `"{not json"` as its failure-injection and expects the run to fail | `EV/vitest-full-1.txt` | — | kept strict: the run fails with "The run options stored on this request are corrupt; start a new run…"; weights/maxCandidates *values* are still sanitised | `ws1-run.test.ts` "a corrupt or hostile snapshot…" | — | pass | VERIFIED_FIXED |
| WS1-F31 | P3 | COVERAGE_GAP (sweep decisions) | Swallowed catches reviewed: `grading.ts:108` (corrupt LlmGrade cache row → re-grade), `llm/client.ts` telemetry writes, `refresh.ts` stamp-on-error (rotates a wedged row), notification `.catch`, `run.ts:54` boss lookup → treated as "no live job" then a failed enqueue is reported. Loops: `http.ts take()` bounded by refill/pause (F22), `library.ts` paging bounded by cursor. No TODO/FIXME. Missing awaits: none (the two bare `prisma.*` calls at `run.ts` 621–622 are inside a `$transaction([...])` array). Permissive fallbacks: `qty ?? 1` (F18, spec), `endorsements ?? 1` (fine) | grep commands in §3 | — | no change | — | — | — | NOT_A_DEFECT_WITH_RATIONALE |
| WS1-F32 | P3 | DATA_QUALITY | Eval sample: curated rows whose expected SKU contradicts the competitor text (C0121 "Cannula Only" → expects trocars 176674PF/B12LGF; C0Q11 "Threaded Cannula and Seal" → expects ONB5LGF trocar; CFI12 "Threaded sleeve Optical Tip with Handle" → expects ONB5LGF) stay misses by design (the hard component constraint holds) | `EV/eval-all-after-det.txt` | sheet inconsistencies (BUILD_NOTES §26.4 lists 11 such rows) | none | — | — | — | OPEN (sheet owner) |

### Evaluation delta (K), explained line by line

Baseline on this DB (fresh copy, first command of the run): resolution 80/80, top-1 **26/80**, top-3 **46/80** (`EV/eval-before.txt`, identical to the `crosswalk_dbg` baseline quoted in the brief). Final: resolution 80/80, top-1 **29/80**, top-3 **46/80**, stable across two consecutive runs (`EV/eval-after.txt`).

Decomposition (per-code files `EV/eval-all-*.txt`, produced by `EV/scripts/eval-all.ts` = `scripts/eval.ts` printing every code; competitor bins were reset to the template between runs so only code changes are measured):

1. **Ordering noise, not my change (F05):** with the original code the score depends on physical row order. After tier3's embedding UPDATEs rewrote every OwnProduct row the same code scored 28/46 (`eval-all-before.txt`); with the pool in SKU order it scores 27/45 (`eval-all-before-det.txt`). Codes that flip purely by order: B11LT (NONB11STF ↔ NB11STF exact tie), CB11LT (UNVCA11STF ↔ NBFCA11ST/ONBFCA11ST tie), TLC10 (GIA10038S ↔ GIA10048S), TLC75/TLC55 (GIA6038S ↔ GIA8038S). The deterministic order is now the SKU order; it is arbitrary on a true tie but reproducible.
2. **Matcher fixes F02/F03 (+2 top-1, +1 top-3 against the deterministic 27/45 → 29/46):**
   - B12SRT (Endopath Xcel bladeless, expected ONB12STF/NONB12STF/NONB12SHF/NB12SHF/ONB12SHF): miss → **T1**. NBFCA12ST (wrongly a trocar) was #1; it is now a cannula (No Match) and NB12SHF (now a trocar) is #1.
   - CTB12LT (Endopath BASX stability sleeves = cannula line, expected NBFCA12ST/UNVCA…): T3 → **T1**. NBFCA12ST is now a cannula and tops the list.
   - Lines that moved inside the top 3 without changing status: 200012151 (NB11STF → NB12STF now a trocar: 12 mm outranks 11 mm), 35LDA (NB5LGF now a trocar), BV-5X100 / CTR12 (ONB5STF-NSB, ONB5STF2C now carry a profile from the SKU rule after the rebuild), C0605 (NB15STF/NONB15STF), C0Q11 / CTS01 / TT012 (NB5LGF/NB12SHF left the cannula lines: they are trocars, hard No Match), CFI12 / C0121 / D12LT (see F32; D12LT's NB12STF #1 is the PACR-win answer documented in `scripts/eval-pacr.ts`).
   - No line regressed from T1 or T3 relative to the deterministic baseline.
3. Unit suites `tests/unit/pacr-regression.test.ts tests/unit/match-quality.test.ts`: 103/103 before (`EV/unit-before.txt`) and 103/103 after (`EV/unit-after.txt`).

## 3. Commands (exit codes and counts)

All with `set -a; . ./.env.local.ws1; set +a` in `/home/claude/cracr`.

| # | command | result |
|---|---|---|
| 1 | `npm run eval -- --n 80 --seed 7 --no-crosses` (baseline, fresh DB) | EXIT 0; 80/80, 26/80, 46/80 (`EV/eval-before.txt`) |
| 2 | `npx vitest run tests/unit/pacr-regression.test.ts tests/unit/match-quality.test.ts` (baseline) | EXIT 0; 103 passed (`EV/unit-before.txt`) |
| 3 | `timeout 600 npm run test:enterprise` (baseline) | EXIT 1; 13 passed, 5 failed (`EV/enterprise-before.txt`) |
| 4 | `psql` KnownCross/OwnProduct/CompetitorProduct queries for B12LTH; `npx tsx EV/scripts/b12lth.ts` | `EV/b12lth-evidence.txt` |
| 5 | `UPDATE "OwnProduct" SET embedding=NULL…`; `npx vitest run tests/db/tier3.test.ts -t "refreshEmbeddings writes vectors once"` | 1 failed (`expected 60 to be +0`, `EV/tier3-before.txt`) |
| 6 | same reset; `npx vitest run tests/db/tier3.test.ts` ×2 (fresh, warm) | EXIT 0 / 0; 17 passed / 17 passed (`EV/tier3-after.txt`) |
| 7 | `npm run eval:gate` | EXIT 1 before and after (BIN_VERSION vs baseline 6; model name) (`EV/eval-gate.txt`) |
| 8 | `npm run eval …` with BIN_VERSION 8 only | 30/47 (`EV/eval-exp1-binbump-only.txt`, bins not reset — superseded by the per-code runs) |
| 9 | `npx tsx EV/scripts/eval-all.ts …` stashed vs current, bins reset, physical order then SKU order | 28/46 → 30/47 (physical), 27/45 → 29/46 (deterministic) (`EV/eval-all-*.txt`) |
| 10 | `npm run eval -- --n 80 --seed 7 --no-crosses` (final) ×2 | EXIT 0; 80/80, 29/80, 46/80 both times (`EV/eval-after.txt`) |
| 11 | `npx tsx prisma/seed-enterprise.ts` | EXIT 0 (`EV/seed-enterprise-after.txt`); NONB12STF list 112.00 / cost 31.00 / 1 GPO entry |
| 12 | `psql`: published-version entries with `ownSku` not in the catalog | 654 rows: DUPLICATE 412, REPEAT 115, HAND 59, DISCONT 57, ALTERNATE 39, ALTERNATIVE 28, … |
| 13 | `npx tsx EV/scripts/intake-probe.ts` before/after | `EV/intake-probe.txt`, `EV/intake-probe-after.txt` |
| 14 | `npx tsx EV/scripts/access-probe.ts`, `component-probe.ts` | `EV/access-probe.txt` |
| 15 | `npx vitest run tests/unit/ws1-intake.test.ts` (pre-fix via `git stash push -- src/lib/excel/intake.ts src/lib/cfn.ts`, then post-fix) | 6 failed / 13 passed → 19 passed (`EV/ws1-intake-prefix.txt`) |
| 16 | `npx vitest run tests/unit/ws1-access.test.ts` (pre-fix stash of match files, then post-fix) | 5 failed / 25 passed → 30 passed (`EV/ws1-access-prefix.txt`) |
| 17 | `npx vitest run tests/unit/ws1-ranking.test.ts` (pre/post) | 8 failed / 14 passed → 22 passed (`EV/ws1-ranking-prefix.txt`) |
| 18 | `npx vitest run tests/db/ws1-run.test.ts` (pre/post) | 4 failed / 5 passed → 9 passed (`EV/ws1-run-prefix.txt`, `EV/ws1-run-after.txt`) |
| 19 | `npx vitest run tests/db/ws1-gudid.test.ts` (pre/post) | 6 failed / 3 passed → 9 passed (`EV/ws1-gudid-prefix.txt`, `EV/ws1-gudid-after.txt`) |
| 20 | `npx vitest run` (full) #1 | EXIT 1; 386 passed, 2 failed (tier1: leftover jobs F28, corrupt optionsJson F30) (`EV/vitest-full-1.txt`) |
| 21 | `npx vitest run` (full) #2, #3, final | EXIT 0; 18 files, **388 passed** each time (`EV/vitest-full-2.txt`, `-3.txt`, `-final.txt`) |
| 22 | `npm run typecheck` | EXIT 0 (`EV/typecheck.txt`) |
| 23 | `npm run check` | EXIT 0; 19 passed (`EV/check.txt`) |
| 24 | `npm run check:enterprise` | EXIT 0; 21 passed (`EV/check-enterprise.txt`) |
| 25 | `timeout 600 npm run test:enterprise` (after seed + fixes; run twice) | EXIT 0; **18 passed, 0 failed**, process exits on its own (`EV/enterprise-after.txt`, `EV/enterprise-final.txt`) |
| 26 | `timeout 600 npm run test:adversarial` | first run EXIT 1 (intake `abc` → 1 policy, F18), after revert EXIT 0; **24 passed** (`EV/adversarial-final.txt`) |
| 27 | `grep` sweep commands (TODO/FIXME, `catch {}`/`.catch(() =>`, `for (;;)`, bare `prisma.*` calls, raw `e.message`) | see F31 |

## 4. Changed files (`git status --short`, my ownership only)

```
 M docs/MATCH_QUALITY_MODEL.md          (§5.1 grade ordering, §5.2 weight snapshot, §5.3 re-run policy, §5.4 preferred notes)
 M prisma/seed-enterprise.ts            (demo SKU list only: NONB12STF)
 M scripts/eval.ts                      (deterministic own-product order)
 M scripts/test-enterprise.ts           (expectations only: B12LTH → NONB12STF)
 M src/lib/cfn.ts                       (cfnVariants removed; 42-char limit)
 M src/lib/excel/intake.ts              (F14–F18)
 M src/lib/gudid/http.ts                (timeout, clock-skew refill)
 M src/lib/gudid/library.ts             (prune idempotence, sanitised import errors)
 M src/lib/gudid/openfda.ts             (memo test seam)
 M src/lib/match/access.ts              (toMm, cm lengths, pack guard)
 M src/lib/match/bin.ts                 (BIN_VERSION 8; negation-aware feature tags)
 M src/lib/match/brands.ts              (versaport-sleeve unless; negation rules)
 M src/lib/match/component.ts           (withCannula rewrite)
 M src/lib/match/grading.ts             (sibling floor respects the cap)
 M src/lib/match/line.ts                (gradeRank, preferred redirect, SKU tiebreak, resolveSelfMatch)
 M src/lib/match/score.ts               (MATCH_ORDER/gradeRank, SKU tiebreak)
 M src/lib/pipeline/run.ts              (weights snapshot, maxCandidates, reviewed re-run policy, SELF_MATCH chain, sanitised errors, parseRecord, pool tiebreak)
 M src/lib/xref/governance.ts           (publishVersion: catalog-only redirects, no placeholder SKUs)
 M tests/db/tier3.test.ts               (3.5 block only)
 M tests/unit/pacr-regression.test.ts   (proxy regex)
?? tests/db/ws1-gudid.test.ts  ?? tests/db/ws1-run.test.ts
?? tests/unit/ws1-access.test.ts  ?? tests/unit/ws1-intake.test.ts  ?? tests/unit/ws1-ranking.test.ts
?? docs/debug-runs/                    (this report + evidence)
```

Full diff: `EV/ws1.diff` (1,062 lines). `src/lib/xref/governance.ts` is the governance service the ownership list names as `src/lib/crosswalk/**` (that path does not exist); the change is confined to `publishVersion`.

## 5. Remaining gaps and what would be needed

- **Re-publish the crosswalk** (F10): every environment whose PUBLISHED version was built before this fix carries note-SKUs (`DUPLICATE`, …) as own SKUs; proposals pin them. Needs a governance user to run `publishVersion` after deploy; WS2 should confirm `approvedCross` consumers cope with the entry count dropping by ~650.
- **Shared-file patches for others:** `prisma/seed.ts` — pass `manufacturer: COMPANY` (and `code: sku`) to `heuristicBin` (F04); `docs/BUILD_NOTES.md` — §9.2 (optionsJson now carries `weights`/`maxCandidates`), §10.4 (sort ends with SKU; downsell rank), §12.1/§26.1 #6 and §26.2 (KN-06, KN-11, KN-14 resolved; F29 exit note stale), §5/`.env.example` (`OPENFDA_TIMEOUT_MS`), §9.1 (intake: 10 non-empty rows, header rows ignored, ≤ 42 chars, unit-suffixed quantities); `tests/db/tier1.test.ts` uses corrupt `optionsJson` as its failure injection (kept working, F30).
- **eval:gate** (F26) needs `OPENAI_API_KEY` to re-measure at bins v8 and accept.
- **Quantity words → 1** (F18) is spec-pinned; a warnings channel would let the UI show "quantity 'abc' read as 1".
- **Thoracoport** component (F27) and the three inconsistent curated rows (F32) need the sheet owner.
- Not covered: HTTP-level intake limits (1–5,000 lines are enforced in `POST /api/requests`, WS4), embeddings retrieval paths with a real model, `scripts/eval-pacr.ts`.
- The eval numbers in BUILD_NOTES §23.3 (42/68 of 120) are for a 120-code sample; the 80-code sample used here is 29/46 after this workstream (26/46 before, of which ±2 was row-order noise).

## 6. Evidence paths

`docs/debug-runs/2026-09-24-full-application/evidence/ws1/`: `eval-before.txt`, `eval-after.txt`, `eval-all-{before,after}{,-det}.txt`, `eval-exp1-binbump-only.txt`, `eval-exp2-rules.txt`, `eval-gate.txt`, `unit-{before,after}.txt`, `enterprise-{before,after,final}.txt`, `adversarial-final.txt`, `check.txt`, `check-enterprise.txt`, `typecheck.txt`, `tier3-{before,after}.txt`, `vitest-full-{1,2,3,final}.txt`, `ws1-{intake,access,ranking,run,gudid}-prefix.txt`, `ws1-{run,gudid}-after.txt`, `b12lth-evidence{,-after}.txt`, `intake-probe{,-after}.txt`, `access-probe.txt`, `own-trocar-components-before.txt`, `seed-enterprise-after.txt`, `git-status.txt`, `ws1.diff`, `scripts/{b12lth,eval-all,intake-probe,access-probe,component-probe}.ts`.
