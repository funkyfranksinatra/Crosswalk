# WS2 report — contracts, money, waterfall, policy/recommendation, proposals, scenarios, drift, tax/freight, approvals, delegation, outcomes, compliance, exports

Branch `debug/2026-09-24-full-application`, env `.env.local.ws2`, DB `crosswalk_ws2` (rebuilt from template `crosswalk_dbg` at the start, then `npx tsx prisma/seed-enterprise.ts` for the NONB12STF demo price as COMMON.md says). No server started; every route handler was exercised in-process. No commits, no pushes, nothing against Neon. Evidence: `docs/debug-runs/2026-09-24-full-application/evidence/ws2/` (`EV/` below; `EV/scripts/` holds the probe scripts, `EV/patches/` the suggested patch for the WS4-owned route).

## 1. Inspected scope

Read in full: `docs/BUILD_NOTES.md` §13–16 (+ §26), `docs/BUSINESS_RULES.md`; `src/lib/money.ts`; `src/lib/contracts/{resolve,context,rebates,bundles,clauses}.ts`; `src/lib/pricing/{policy-model,policy,recommend}.ts`; `src/lib/proposals/{service,economics,drift,export,outcome}.ts`; `src/lib/approvals/{rules,service,delegation}.ts`; `src/lib/tax/{index,avatax,manual,types}.ts`; `src/lib/compliance/index.ts`; `src/lib/catalog/{cost,fx}.ts`; `src/lib/excel/export.ts`; `src/lib/pdf/{index,documents}.ts`; `src/lib/sheets/csv.ts`; `src/lib/intelligence/{index,summarize}.ts` (headers + basis rules); `src/lib/analytics/{index,snapshots}.ts` (sweep only); the WS4/WS5-owned files needed to reason about my area, read-only: `src/app/api/contracts/[id]/entries/route.ts`, `src/app/api/proposals/[id]/{scenarios/**,export,push-crm}/route.ts`, `src/lib/api.ts`, `src/lib/auth/{permissions,scope}.ts`, `src/lib/audit.ts`, `src/lib/branding.ts`, `src/lib/db/constraints.ts`, `src/lib/integrations/{sync.ts,salesforce/writeback.ts,fx/service.ts}`, `src/lib/notifications/index.ts`, `src/lib/pipeline/run.ts` (`priceOf` only), `prisma/schema.prisma` (commercial models), `scripts/{check-enterprise,test-enterprise}.ts`.

Behaviours exercised (mandatory items A–L): A contract price-entry writes; B scenario kinds; C money oracle + float grep; D waterfall table + run-vs-proposal parity; E cost & FX; F policy/recommendation + activation race; G economics reconciliation vs stored JSON, xlsx/csv/pdf and CRM payload; H drift triggers + freight/tax + AvaTax fake + fingerprint + export gate; I approvals (atomic submit with per-step failure injection, races, stale snapshots, reopen, derived status, self-decision, break-glass, delegation); J finalisation, WON/LOST/NO_DECISION, compliance, rebates, bundles, renewals, conversion; K export content incl. sentinel leak search and CSV injection; L code sweep + the three npm suites before/after.

Not done: no HTTP/browser tests (routes are WS4's, pages WS3's — the two route handlers I did call in-process were the scenario PATCH and, for evidence only, the contracts entries POST); `src/lib/intelligence/bids.ts` (SAM.gov) and `src/lib/analytics/index.ts` were only swept, not tested; GROWTH_PCT rebate measurement (needs a prior-period baseline the model does not carry — left as an open gap); the WS4-owned entries route is **not** patched (patch supplied, F01 stays FIXED_AWAITING_VERIFICATION until applied).

## 2. Findings

| id | sev | type | title | reproduction | root cause | fix (files) | regression test | pre-fix evidence | post-fix | status |
|---|---|---|---|---|---|---|---|---|---|---|
| WS2-F01 | P1 | DEFECT | KN-18: contract price-entry batch is non-transactional — a failure on row 2 of 3 leaves row 1 half-applied, row 2's old ACTIVE price superseded **with no replacement** (the SKU now has no price), row 3 untouched, and no audit event | `VITEST=1 npx tsx EV/scripts/kn18-probe.ts` (route handler in-process, 2nd `priceEntry.create` throws): `A → SUPERSEDED:90, ACTIVE:80 · B → SUPERSEDED:90 · C → ACTIVE:90 · audit events: 0` (`EV/kn18-probe-before.txt`) | the whole write loop lives in the route; per-row `updateMany` + `create`, no transaction, audit only at the end | new `src/lib/contracts/entries.ts` (`addContractEntries`): validate every row and resolve every SKU first, then ONE interactive transaction — `SELECT … FOR UPDATE` on the contract row (per-contract serialisation), supersede + create per band, audit event with the real counts inside the tx; `entriesAdapterSupported()` refuses `neon-http` up front (no half-write); duplicate SKU+band+date rows in one batch refused | `tests/db/ws2-contracts.test.ts` (5 cases: mid-batch failure → state identical, no SUPERSEDED, no audit; clean batch counts + audit; 2 concurrent batches → 1 ACTIVE band; full validation matrix; neon-http refusal) | `EV/kn18-probe-before.txt`; concurrency probe `EV/kn18-concurrent-before.txt` did **not** reproduce duplicates in-process (narrow window) — the lock is still required | `EV/ws2-contracts-after.txt` 5/5; route re-probed after: still partial (`EV/kn18-probe-after-route-unpatched.txt`) because the route is unchanged | **FIXED_AWAITING_VERIFICATION** — WS4 must replace `src/app/api/contracts/[id]/entries/route.ts` with `EV/patches/contracts-entries-route.ts` (5 lines; body/response unchanged) |
| WS2-F02 | P2 | DEFECT | KN-12: scenario kinds disagree — service accepted 4, the CHECK constraint 6, the UI offers CUSTOMER_REQUESTED → "kind must be RECOMMENDED, AGGRESSIVE, MARGIN_OPTIMIZED or CUSTOM" from the workspace | `npx vitest run tests/db/ws2-proposals.test.ts` against the original service: 3 failed (`EV/ws2-proposals-prefix.txt`: `SCENARIO_KINDS is not iterable`, `kind must be RECOMMENDED, …`) | three lists | `src/lib/proposals/service.ts`: `SCENARIO_KINDS` (6, equal to the constraint) with documented seeding — CUSTOMER_REQUESTED = the customer's current competitor price where known else the proposed price; FINAL = copy of proposed prices; rule text in `docs/BUSINESS_RULES.md`. Route needs **no** patch (it passes `kind ?? "CUSTOM"` through); constraint needs **no** change | `ws2-proposals.test.ts` "KN-12 …" (asserts service list == `ENUM_CONSTRAINTS` Scenario.kind) + one case per kind (AGGRESSIVE 855 / 363.64 / 285, MARGIN_OPTIMIZED 727.27 / 181.82, CUSTOMER_REQUESTED, CUSTOM, FINAL, RECOMMENDED) | above | `EV/ws2-proposals-after.txt` 8/8 | VERIFIED_FIXED |
| WS2-F03 | P2 | DEFECT | Scenario prices had weaker bounds than manual prices: `setScenarioPrice` had no ≤ 1e9 / ≤ 10 × list check, `applyScenario` no ≤ 1e9 check — a stored scenario price of 3001 on a 300-list line could be applied | `ws2-proposals.test.ts` "scenario prices and manual prices share one set of bounds" pre-fix: `promise resolved … instead of rejecting` (`EV/ws2-proposals-prefix.txt`) | three copies of the validation | `service.ts` `boundedPrice()` — one implementation for `setProposedPrice`, `setScenarioPrice`, `applyScenario` (positive, ≤ MAX_UNIT_PRICE, ≤ 10 × list, rounded half-even to the minor unit; null clears) | same test (+ null clears, 123.455 → 123.46, apply refused and proposal untouched) | above | pass | VERIFIED_FIXED |
| WS2-F04 | P2 | DEFECT | `money("NaN")` / `money("Infinity")` returned a non-finite Decimal: NaN compares false to every bound so `positiveMoney`/`lte(0)`/`gt(max)` pass it, `toDb` gives `"NaN"`, and PostgreSQL stores `numeric NaN` (`psql: select 'NaN'::numeric(18,4)` → `NaN`) — a typed "NaN" freight value or scenario price would land in a price column | `npx vitest run tests/unit/ws2-money.test.ts` pre-fix: `expected NaN to be null` (`EV/ws2-money-prefix.txt`); `EV/scripts` probe: `lte(0): false gt(1e9): false toDb: NaN` | decimal.js parses the strings "NaN"/"Infinity" | `src/lib/money.ts` `money()`: non-finite → null in every form (number, string, Decimal) | `ws2-money.test.ts` (8 cases: precision/rounding facts, half-way cases, Decimal(18,4)/(12,6) bounds and clamp, null vs zero, NaN/±Infinity/overflow, serialisation, formatting) | above | `EV/ws2-money-after.txt` 8/8; the tax test asserts `freightValue: "NaN"` is refused, the scenario route returns 400 "not a number" | VERIFIED_FIXED |
| WS2-F05 | P2 | DEFECT | Run-vs-proposal pricing divergence: a run without a selected price book passes **no** price-book entries to the waterfall (`run.ts priceOf`), the proposal passed **every** price book's entries — a SKU without a catalog list price showed "no price" to the rep and was then priced from an arbitrary price book on the proposal (77 → proposed 18.18 at target margin) | `npx tsx EV/scripts/pricebook-divergence-probe.ts`: `run priceOf: null` vs `proposal snapshot listPrice: 77` (`EV/pricebook-divergence-before.txt`) | `createFromRequest` / `drift.ts` used `product.prices` unfiltered | `service.ts` `listEntriesFor(prices, request.pricebookId)` (only the request's price book; none → none) used by creation and by `drift.ts` (both `driftFor` and `refreshContext`); rule in BUSINESS_RULES "Price waterfall 1." | `tests/db/ws2-waterfall.test.ts` "LIST from a pricebook: only the request's selected pricebook …" + the parity test "the same SKU / account / quantity resolves identically through the run's context and the proposal's snapshot" (LOCAL and a GPO volume band, step-by-step) | above | `EV/pricebook-divergence-after.txt`: both null; `EV/ws2-waterfall-after.txt` 52/52 | VERIFIED_FIXED |
| WS2-F06 | P2 | DEFECT | Policy activation race: two drafts for one family activated concurrently → **two ACTIVE policies** (the batch `$transaction([updateMany, update])` evaluates `status='ACTIVE'` against a snapshot in which neither draft is active); also any status (SUPERSEDED, ACTIVE) could be "activated" again | `npx vitest run tests/db/ws2-policy.test.ts` pre-fix: `round 1: A1,B1: expected 2 to be 1`; re-activation `resolved instead of rejecting` (`EV/ws2-policy-db-prefix.txt`) | no serialisation per family; no status check | `src/lib/pricing/policy.ts` `activatePolicy`: interactive tx, `pg_advisory_xact_lock(hashtext('pricing-policy:<family>'))`, DRAFT-only (re-checked inside the lock), audit in the tx | `ws2-policy.test.ts` (versioning/merge, every invalid definition rejected before any write, DRAFT-only, 5 rounds of concurrent activation → exactly one ACTIVE) | above | `EV/ws2-policy-db-after.txt` 3/3 | VERIFIED_FIXED |
| WS2-F07 | P2 | DEFECT | `explanationPublic` leaked the floor figure: a competitor price below floor produced "raised to the floor $571.43" in the public text (floor = cost ÷ (1 − min margin), i.e. cost-derived); and when the floor exceeds the reference the note said "raised to the floor" although the price was then capped below it | `npx vitest run tests/unit/ws2-policy.test.ts` pre-fix: sentinel `12345.68` present in `explanationPublic`; floor-above-list case explanation wrong (`EV/ws2-policy-prefix.txt`) | one `notes` list served both explanations; clamp note computed from `before.lt(floor)` only | `src/lib/pricing/recommend.ts`: `notesPublic` ("raised to the pricing floor", no amount); explicit inconsistent-inputs branch ("floor $F is above list $L; capped at list and flagged below floor" / public "capped at list $L; below the pricing floor"); rules in BUSINESS_RULES | `ws2-policy.test.ts` "explanationPublic never contains cost, floor or margin figures (sentinel values)" (6 shapes) and "inconsistent inputs: floor above the reference …" | above | `EV/ws2-policy-after.txt` 36/36 | VERIFIED_FIXED |
| WS2-F08 | P2 | DEFECT | Stored deal economics carried stale approval counts: `submitForApproval` rolled up **before** routing (`approvalsPending: 0` with a PENDING line); `refreshContext` rolled up before `recomputeAllLines`; `decide`/`reopen` never re-rolled | `npx tsx EV/scripts/econ-stale-probe.ts`: `line approvalState: PENDING | economicsJson.approvalsPending: 0` (`EV/econ-stale-before.txt`); `EV/ws2-drift-tax-prefix.txt` `expected +0 to be 1`; `EV/ws2-approvals-prefix.txt` | ordering | `approvals/service.ts` (re-roll inside the submit/decide transactions and after reopen), `proposals/drift.ts` (second `refreshEconomics` after the recompute) | `ws2-approvals.test.ts` "routing on submission …" (`approvalsPending: 2`), "deciding …"; `ws2-drift-tax.test.ts` "refreshContext …" (`approvalsRequired: 1`) | above | `EV/econ-stale-after.txt`: `approvalsPending: 1`; suites green | VERIFIED_FIXED |
| WS2-F09 | P1 | DEFECT | Submission was not atomic: manual rollback in `catch`, but the "withdraw earlier requests" step ran **before** the `try` (a failure there left the proposal locked + SUBMITTED) and withdrawn earlier requests were never restored; decision tail (line state, proposal status, audits) was not atomic either; a decision could overwrite a DRAFT (reopen race) or resurrect a LOST proposal that still had a PENDING request | code reading; `ws2-approvals.test.ts` pre-fix: 4 failed (`EV/ws2-approvals-prefix.txt` — the per-step failure injection has no transaction to hook: `.toMatch() expects a string, got undefined`) | no `$transaction` | `approvals/service.ts`: `submitForApproval` = one interactive tx (claim, withdraw, recompute via tx-aware `recomputeAllLines`/`refreshEconomics` (new optional `db` param in `proposals/service.ts`), routing, requests, status, economics, audit via `auditIn(tx, …)`); `decide` = pre-check (DRAFT/WON/LOST → "no longer open for decision") + one tx from the claim to the audit, proposal status written with `updateMany … status in <approval cycle>`; `recordOutcome(LOST)` withdraws PENDING requests | `ws2-approvals.test.ts` "submission is atomic: a forced failure at each step …" (7 injection points via `withTxFailure`, state identical after each), "two simultaneous submits → one wins …", "deciding … simultaneous approve + reject → exactly one decision; a retry is refused", "a stale snapshot cannot authorise a changed price … reopen …" (incl. DRAFT guard) | above | `EV/ws2-approvals-after.txt` 10/10 | VERIFIED_FIXED |
| WS2-F10 | P1 | DEFECT | WON was not atomic and not race-safe: contract in one tx, then outcome upsert, then proposal update (a failure after the contract left a contract without a WON); two simultaneous closes both passed the status check → the loser died with a raw Prisma unique-constraint dump (`Invalid prisma.contract.create()`); months validated only after the idempotency lookup; `finalValue: "abc"` silently closed the deal with `finalValue = null`; LOST left PENDING requests decidable | `npx vitest run tests/db/ws2-outcomes.test.ts` against the original: 5 failed (`EV/ws2-outcomes-prefix.txt`: `finalValue "abc"` resolved; `expected 'Invalid prisma.contract.create()…' to match /already (won|lost|closed)/`; `expected ['PENDING'] to equal ['WITHDRAWN']`) | sequential writes, no claim | `src/lib/proposals/outcome.ts`: validation first (outcome, months 1–120 integer, finalValue non-negative number), then ONE tx — atomic claim `updateMany where status notIn [WON, LOST]`, `contractFromProposalIn(tx)` (contract + entries + commitments + audit, idempotent on the contract number), LOST withdraws PENDING, outcome upsert, audit; observations and the analytics refresh after the close; `contractFromProposal` kept (tx-wrapped, validates first) | `ws2-outcomes.test.ts` "WON: refused until finalisable; then one transaction …" (+ waterfall now LOCAL, repeat requests, idempotency), "WON is atomic: a failure after the contract is created rolls … back" (two injection points), "simultaneous closes (WON + WON, WON + LOST) → exactly one wins", "LOST … withdraws pending requests; NO_DECISION leaves the proposal open; one outcome row" | above | `EV/ws2-outcomes-after.txt` 11/11 | VERIFIED_FIXED |
| WS2-F11 | P3 | DEFECT | Tax fingerprint ignored the ship-to actually taxed: with no proposal ship-to, `calculateProposalTax` uses the **account's** default address, but the fingerprint only covered `proposal.shipToJson` — changing the account default did not make the figure stale | `EV/ws2-drift-tax-prefix.txt` "the tax fingerprint goes stale …": `expected true to be false` | `taxFingerprint(p)` on the proposal row only | `src/lib/tax/index.ts` `effectiveShipToJson()` used by `quoteTotals` (loads the account) and `calculateProposalTax` | `ws2-drift-tax.test.ts` "the tax fingerprint goes stale on price / quantity / inclusion / freight / settings / ship-to changes, and NOT on unrelated changes" (+ pure fingerprint cases) | above | `EV/ws2-drift-tax-after.txt` 15/15 | VERIFIED_FIXED |
| WS2-F12 | P3 | DEFECT | Validity contradiction: contract-offer workbook/CSV hard-coded "Valid 90 days", the offer PDF used Branding `validityDays` (default 60), `createFromRequest` hard-coded 60 | `EV/ws2-exports-prefix.txt`; grep `Valid 90` | three sources | `src/lib/excel/export.ts` `validityLine(branding.validityDays)` (`"Valid N days (through <date>)"`), `proposals/service.ts` default `validDays` ← `getBranding().validityDays`; the Settings → Branding "Quote validity (days)" field is the one contract; no UI text contradicts it (grep in `src/app`) | `ws2-exports.test.ts` "contract offer …" (workbook, CSV and PDF validity from branding) and "quote …" (a proposal created without validDays gets `validityDays`) | above | `EV/ws2-exports-after.txt` 5/5 | VERIFIED_FIXED |
| WS2-F13 | P3 | DEFECT | Offer workbook/CSV printed an unpriced matched line with an empty price cell while the offer PDF omitted it and counted it as "not included" — the two customer artefacts disagreed on what is on the offer | `EV/ws2-exports-after.txt` (intermediate run): xlsx 2 not included vs PDF 3 | `offerRows` predicate lacked `unitPrice != null` | `excel/export.ts` `offerRows`: same predicate as the PDF (matched **and** priced); unpriced matches listed for follow-up; the rep's cross-reference workbook still shows them as "No price on file" with empty cells (never 0) | `ws2-exports.test.ts` "contract offer …" and "a stored price of 0 … a missing one as empty" | — | pass | VERIFIED_FIXED |
| WS2-F14 | P3 | DEFECT | Quote/offer PDF code and SKU columns (66 pt, Courier 8 pt ≈ 12 characters) wrap a 13+ character code mid-word (a 14-digit GUDID DI or "PROXIMATE-TX60B" would break across two lines; `pdftotext` de-hyphenates it) | `EV/ws2-economics-after.txt` (intermediate): `ws2muf9o004C` / `1` on two lines | column widths | `src/lib/pdf/documents.ts` COLS 80/116/80/116/30/44/50 (15 characters) | covered by `ws2-exports` / `ws2-economics` PDF assertions (normalised comparison retained) | — | pass | VERIFIED_FIXED |
| WS2-F15 | P3 | DEFECT / COVERAGE_GAP | Rebate performance measured COMPLIANCE_PCT and GROWTH_PCT as 0 always (`rebateBasisValue(r.basis, units, spend, null, null)`) — a compliance rebate never reached a tier | `EV/ws2-outcomes-prefix.txt`: `expected {…} to match object { measured: '0.48', rebate: '0' }` | percentages never computed | `src/lib/compliance/index.ts`: COMPLIANCE_PCT = delivered ÷ committed over the commitments the rebate covers (units, else value); GROWTH_PCT still 0 — no prior-period baseline on the contract (documented in BUSINESS_RULES) | `ws2-outcomes.test.ts` "rebates in performance: highest satisfied tier per basis (UNITS, VALUE, COMPLIANCE_PCT) …" | above | pass | VERIFIED_FIXED (compliance) / OPEN (growth: needs a baseline field or prior-period purchase window — WS5/schema) |
| WS2-F16 | P3 | SPEC_AMBIGUITY | Scenario creation / what-if edits are allowed on a locked (submitted/approved) proposal; only `applyScenario` and price edits are refused. BUILD_NOTES says "use after lock/submission refused" for edits; a read-only what-if on a locked deal is harmless and the workspace offers it | `ws2-proposals.test.ts` "applying a scenario … refused once the proposal is locked" pins: create allowed, apply refused | — | none (kept) | same | — | — | NOT_A_DEFECT_WITH_RATIONALE |
| WS2-F17 | P3 | DEFECT (not mine) | CRM push (`src/lib/integrations/sync.ts pushQuote`, `salesforce/writeback.ts buildQuoteWriteback`) gates on `status ∈ {APPROVED, WON}` only, not on `canFinalize` (BUILD_NOTES §16.5 says it needs `canFinalize`): an APPROVED proposal past `validThrough`, or one whose included line lost its price after approval, can still be pushed. Neither CRM push nor WON require a fresh tax figure — only the quote exports do (`buildQuote`/`buildQuotePdf`), which is consistent with "tax is never part of line economics"; I did not broaden the policy | code reading (`grep -n finalizeCheck src/lib/integrations/*`: none) | — | WS5 file — suggested patch: in both functions replace the status check with `const f = await finalizeCheck(proposalId); if (!f.ok) throw new ValidationError(...)` | — | — | — | OPEN (WS5) |
| WS2-F18 | P3 | DEFECT (not mine) | `src/lib/pipeline/run.ts priceOf` stores the waterfall price as a JS `Number(r.price.toString())` on `MatchCandidate.unitPrice` (a Decimal(18,4) column) — a float on a money path. Values are exact at 4 dp for realistic prices (asserted in the parity test), so no observable defect; noted as the one float leak in the grep (`parseFloat|Number(|toFixed|* 100|/ 100`) besides display/transport uses | `grep` in §3; `ws2-waterfall.test.ts` parity asserts `new Decimal(Number(price)).toFixed(4) === price.toFixed(4)` | — | WS1 file — suggested: `unitPrice: r.price.toString()` and keep `num()` only for the ranker | — | — | — | OPEN (WS1, cosmetic) |
| WS2-F19 | P3 | NOT_A_DEFECT (sweep) | Float/`Number`/`toFixed` uses on my paths are all display or transport: `money.ts num()/fmt()`, `recommend.ts` `discount.toNumber()` with a 1e-9 tolerance in an authority *comparison* (pinned: 0.150000001 → rep, 0.15001 → manager), `policy-model.ts` margins as numbers (policy parameters, not money), `avatax.ts` `Number(amount)` in the provider's JSON body (AvaTax requires numbers; the reply is re-read as strings → Decimal), `manual.ts rate.toNumber()` for the rate display, `pdf/documents.ts` `Number(v)` for Intl formatting of already-rounded strings, `export.ts` `Math.round(confidence*100)/100` (a score), `clauses.ts` years as float (a date fraction). Catch blocks: JSON parses with typed fallbacks, notification `.catch(() => undefined)` (documented best-effort), `createFromRequest` cleanup `.catch`, analytics snapshot prune `.catch` (best-effort cache). No TODO/FIXME, no `while` loops, no unawaited Prisma calls | §3 grep commands | — | none | — | — | — | NOT_A_DEFECT_WITH_RATIONALE |
| WS2-F20 | P3 | NOT_A_DEFECT | A selected "No Match" candidate is shown in the rep's cross-reference workbook with its SKU and match type "No Match" (the "NO MATCH" cell is only for lines with no selection); the offer/quote never print it | `ws2-exports.test.ts` "cross-reference workbook …" pins it | legacy BAT convention | none | — | — | — | NOT_A_DEFECT_WITH_RATIONALE |
| WS2-F21 | P3 | COVERAGE_GAP | Pre-existing: `Decimal(18,4)` amounts are never clamped by `toDb` (documented) — a 1e9 unit price × 1e6 quantity would overflow the column in a raw write; every entry point bounds prices at 1e9 (`positiveMoney`, `boundedPrice`, `validateEntryRows`) and quantities come from intake (≤ 10 M), so the largest extended value (1e16) is only computed, never stored | `ws2-money.test.ts` pins `toDb("1e15")` → 16-digit string (DB refuses) | — | none | — | — | — | NOT_A_DEFECT_WITH_RATIONALE |

Documentation: `docs/BUSINESS_RULES.md` updated (money non-finite rule and column precisions, price-book basis, floor-above-reference behaviour, public explanation, transactional submission/decision, break-glass, economics re-roll, scenario kinds and bounds, validity, transactional entries, compliance basis, WON atomicity, tax fingerprint).

## 3. Commands (chronological; exit codes; counts)

| # | command | exit | result / evidence |
|---|---|---|---|
| 1 | `psql … DROP DATABASE crosswalk_ws2; CREATE DATABASE crosswalk_ws2 TEMPLATE crosswalk_dbg` | 0 | rebuilt |
| 2 | `npm run check:enterprise` (before) | 0 | 21/21 `EV/check-enterprise-before.txt` |
| 3 | `npm run test:enterprise` (before re-seed) | 1 | 15/18 — 3 failed on the missing NONB12STF demo price (as COMMON.md warns) |
| 4 | `npx tsx prisma/seed-enterprise.ts` | 0 | `EV/seed-enterprise.txt` |
| 5 | `npm run test:enterprise` (before, re-seeded) | 0 | 18/18 `EV/test-enterprise-before.txt` |
| 6 | `npm run test:adversarial` (before) | 0 | 24/24 `EV/test-adversarial-before.txt` |
| 7 | `npx tsx …/money-probe.ts` (scratch) | 0 | money facts used to write the oracle |
| 8 | `npx vitest run tests/unit/ws2-money.test.ts` (pre-fix) | 1 | 7/8, `expected NaN to be null` `EV/ws2-money-prefix.txt` |
| 9 | `psql: select 'NaN'::numeric(18,4)` | 0 | `NaN` (stored) |
| 10 | `npx vitest run tests/unit/ws2-money.test.ts` (after) | 0 | 8/8 `EV/ws2-money-after.txt` |
| 11 | grep `parseFloat\|Number(\|toFixed\|\* 100\|/ 100\|.toNumber()` over owned dirs | 0 | F18/F19 |
| 12 | `VITEST=1 npx tsx EV/scripts/kn18-probe.ts` (route, pre-fix) | 0 | partial write, 0 audit `EV/kn18-probe-before.txt` |
| 13 | `npx vitest run tests/db/ws2-contracts.test.ts` | 0 (after one expectation fix) | 5/5 `EV/ws2-contracts-after.txt` |
| 14 | `VITEST=1 npx tsx EV/scripts/kn18-concurrent-probe.ts` | 0 | 1 ACTIVE (not reproduced in-process) `EV/kn18-concurrent-before.txt` |
| 15 | `npx vitest run tests/db/ws2-proposals.test.ts` (original service swapped in) | 1 | 3 failed `EV/ws2-proposals-prefix.txt` |
| 16 | `npx vitest run tests/db/ws2-proposals.test.ts` (after) | 0 | 8/8 `EV/ws2-proposals-after.txt` |
| 17 | `npx vitest run tests/db/ws2-waterfall.test.ts` | 0 | 51/51 then 52/52 after the price-book case `EV/ws2-waterfall-after.txt` |
| 18 | `npx tsx EV/scripts/pricebook-divergence-probe.ts` before / after | 0 / 0 | 77 vs null → null vs null `EV/pricebook-divergence-{before,after}.txt` |
| 19 | `npx vitest run tests/db/ws2-cost-fx.test.ts` | 0 | 8/8 `EV/ws2-cost-fx-after.txt` |
| 20 | `npx vitest run tests/unit/ws2-policy.test.ts` (pre-fix) | 1 | 3 failed `EV/ws2-policy-prefix.txt` |
| 21 | `npx vitest run tests/unit/ws2-policy.test.ts` (after) | 0 | 36/36 `EV/ws2-policy-after.txt` |
| 22 | `npx vitest run tests/db/ws2-policy.test.ts` (pre-fix) | 1 | race reproduced round 1 `EV/ws2-policy-db-prefix.txt` |
| 23 | `npx vitest run tests/db/ws2-policy.test.ts` (after) | 0 | 3/3 `EV/ws2-policy-db-after.txt` |
| 24 | `npx vitest run tests/db/ws2-economics.test.ts` | 0 | 4/4 `EV/ws2-economics-after.txt` |
| 25 | `npx tsx EV/scripts/econ-stale-probe.ts` before / after | 0 / 0 | `approvalsPending: 0` → `1` `EV/econ-stale-{before,after}.txt` |
| 26 | `npx vitest run tests/db/ws2-drift-tax.test.ts` (original tax/drift/approvals swapped in) | 1 | 2 failed `EV/ws2-drift-tax-prefix.txt` |
| 27 | `npx vitest run tests/db/ws2-drift-tax.test.ts` (after) | 0 | 15/15 `EV/ws2-drift-tax-after.txt` |
| 28 | `npx vitest run tests/db/ws2-approvals.test.ts` (original services) | 1 | 4 failed `EV/ws2-approvals-prefix.txt` |
| 29 | `npx vitest run tests/db/ws2-approvals.test.ts` (after) | 0 | 10/10 `EV/ws2-approvals-after.txt` |
| 30 | `npx vitest run tests/db/ws2-outcomes.test.ts` (original outcome/compliance) | 1 | 5 failed `EV/ws2-outcomes-prefix.txt` |
| 31 | `npx vitest run tests/db/ws2-outcomes.test.ts` (after) | 0 | 11/11 `EV/ws2-outcomes-after.txt` |
| 32 | `npx vitest run tests/db/ws2-exports.test.ts` (original export/service) | 1 | 1 failed `EV/ws2-exports-prefix.txt` |
| 33 | `npx vitest run tests/db/ws2-exports.test.ts` (after) | 0 | 5/5 `EV/ws2-exports-after.txt` |
| 34 | sweep greps (catch / TODO / while / unawaited) over owned dirs | 0 | F19 |
| 35 | `npx vitest run <all 12 ws2 files>` | 0 | 12 files, 165/165 `EV/ws2-all-after.txt` |
| 36 | `npm run check:enterprise` (after) | 0 | 21/21 `EV/check-enterprise-after.txt` |
| 37 | `npm run test:enterprise` (after) | 0 | 18/18 `EV/test-enterprise-after.txt` |
| 38 | `npm run test:adversarial` (after) | 0 | 24/24 `EV/test-adversarial-after.txt` |
| 39 | `npx vitest run` (full, #1) | 0 | 30 files, 553/553 `EV/vitest-full-1.txt` |
| 40 | `npm run typecheck` | 0 | `EV/typecheck.txt` |
| 41 | `npx vitest run tests/db/ws2-exports.test.ts tests/db/ws2-economics.test.ts` (after the PDF column change) | 0 | 9/9 `EV/ws2-exports-econ-after2.txt` |
| 42 | `npx vitest run` (full, #2, after BUSINESS_RULES + PDF edits) | 0 | 30 files, 553/553 `EV/vitest-full-2.txt` |
| 43 | `npm run typecheck` (final) | 0 | `EV/typecheck.txt` |
| 44 | `npm run test:enterprise` (final) | 0 | 18/18 `EV/test-enterprise-final.txt` |
| 45 | `VITEST=1 npx tsx EV/scripts/kn18-probe.ts` (route still unpatched) | 0 | still partial `EV/kn18-probe-after-route-unpatched.txt` (F01 awaiting WS4) |

Baseline on entry (after the documented re-seed): check:enterprise 21/21, test:enterprise 18/18, test:adversarial 24/24, full vitest 18 files / 388 tests (WS1's state). Final: 21/21, 18/18, 24/24, 30 files / 553 tests (388 + 165 WS2), typecheck 0 — twice in a row.

Note on evidence hygiene: while capturing pre-fix runs I swapped original files in from `git show HEAD:` and back; a basename collision in my scratch copies (two `service.ts`) silently reverted `src/lib/approvals/service.ts` to HEAD once. I detected it in the sweep (no `auditIn`), re-applied the same patch, verified every fix marker per file (§4), and re-ran the full suite twice afterwards (#42–44).

## 4. Changed files (`git status --short`, WS2 only)

```
 M docs/BUSINESS_RULES.md
 M src/lib/approvals/service.ts        transactional submit / decide, closed-proposal guard, economics re-roll, reopen re-roll
 M src/lib/compliance/index.ts         COMPLIANCE_PCT rebate basis from commitments
 M src/lib/excel/export.ts             validityLine (branding), offer predicate = PDF predicate
 M src/lib/money.ts                    non-finite → null
 M src/lib/pdf/documents.ts            code/SKU columns 80 pt
 M src/lib/pricing/policy.ts           advisory-locked, DRAFT-only activation in one tx
 M src/lib/pricing/recommend.ts        notesPublic, floor-above-reference branch
 M src/lib/proposals/drift.ts          request price book only, economics after recompute
 M src/lib/proposals/outcome.ts        atomic WON/LOST/NO_DECISION close, validation first, LOST withdraws pending
 M src/lib/proposals/service.ts        SCENARIO_KINDS (6), boundedPrice, listEntriesFor, Db-aware refreshEconomics/recomputeAllLines, validity from branding
 M src/lib/tax/index.ts                effectiveShipToJson in the fingerprint
?? src/lib/contracts/entries.ts        transactional price-entry service (KN-18)
?? tests/db/ws2-fixtures.ts            isolated fixtures, PDF text, withTxFailure
?? tests/db/ws2-{approvals,contracts,cost-fx,drift-tax,economics,exports,outcomes,policy,proposals,waterfall}.test.ts
?? tests/unit/ws2-{money,policy}.test.ts
?? docs/debug-runs/…/evidence/ws2/**, workstreams/WS2-report.md
```

Not edited (other owners): `src/app/api/contracts/[id]/entries/route.ts` (WS4 — patch in `EV/patches/contracts-entries-route.ts`), `src/lib/integrations/**` (WS5 — F17), `src/lib/pipeline/run.ts` (WS1 — F18), `src/lib/db/constraints.ts` (WS5 — no change needed for KN-12).

## 5. Remaining gaps

1. **F01 needs WS4** to swap the route body for the five-line delegation in `EV/patches/contracts-entries-route.ts`; until then the API still half-writes on failure (re-probed, `EV/kn18-probe-after-route-unpatched.txt`). After that, `tests/db/ws2-contracts.test.ts` covers the service and the tier-0 route tests still apply to the handler.
2. **F17 (WS5)**: CRM push should gate on `finalizeCheck`, not on status alone. Whether a WON / CRM push should also require a fresh tax figure is a policy question I did not decide (tax is quote-level only today).
3. **GROWTH_PCT rebates** measure 0: the contract has no prior-period baseline (F15). Needs either a `baselineJson` on `RebateSchedule` or a prior-window purchase query keyed on `periodMonths` — schema/WS5.
4. The in-process concurrency probe for the *route* did not reproduce duplicate ACTIVE bands (single process, one pool); the service-level test proves the lock; a two-process test would need the WS3 server.
5. Break-glass can only arise when a requester later gains ADMIN (an ADMIN's own lines are auto-approved on submission); the test models exactly that. Whether an ADMIN should ever be a *requester* of a routed request is a design question, not a defect.
6. `explanationPublic` still names the strategy ("priced at target margin") and quotes discounts from list/contract — intended per BUILD_NOTES §14.2; only figures derived from cost are withheld.
7. Not exercised: `intelligence/bids.ts` (SAM.gov feed) and `analytics/index.ts` beyond the sweep; `contracts/clauses.ts protectedCeiling` beyond the existing check-enterprise cases.

## 6. Evidence paths

`docs/debug-runs/2026-09-24-full-application/evidence/ws2/` — before/after outputs of every suite and script listed in §3 (`*-prefix.txt` = against the original code, `*-after.txt` = fixed), probe scripts under `scripts/` (`kn18-probe.ts`, `kn18-concurrent-probe.ts`, `pricebook-divergence-probe.ts`, `econ-stale-probe.ts`), the WS4 route patch under `patches/`, `vitest-full-{1,2}.txt`, `typecheck.txt`, `check-enterprise-{before,after}.txt`, `test-enterprise-{before,after,final}.txt`, `test-adversarial-{before,after}.txt`.
