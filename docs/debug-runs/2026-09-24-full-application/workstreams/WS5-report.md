# WS5 report — platform: integrations, jobs, feeds, notifications, observability, migrations, retention, secrets/config, tenancy, deployment, CI, docs

Branch `debug/2026-09-24-full-application`, env `.env.local.ws5`, DB `crosswalk_ws5` (rebuilt from `crosswalk_dbg`, then `seed-enterprise.ts`), scratch databases `crosswalk_ws5_empty` (migration chain), `crosswalk_ws5_viol` (pre-upgrade constraint fixture), `crosswalk_ws5_acme` (KN-16), `crosswalk_ws5_ci` (CI mirror), `crosswalk_ws5_restore` (backup drill). No server started, no commits, no pushes, nothing against Neon. Evidence: `docs/debug-runs/2026-09-24-full-application/evidence/ws5/` (`EV/` below; probe scripts in `EV/scripts/`).

## 1. Inspected scope

Read in full: `docs/BUILD_NOTES.md` §4, 5, 6, 18, 19, 22–27; `docs/INTEGRATION_ARCHITECTURE.md`, `docs/OPERATIONS.md`, `docs/DEPLOYMENT.md`, `docs/INTEGRATION_SETUP.md`, `docs/INTEGRATIONS.md`, `docs/BACKUPS.md`, `CONTRIBUTING.md`, `.env.example`, `.github/workflows/ci.yml`, `Dockerfile`, `deploy/*`; `src/lib/jobs/*`, `src/lib/feeds/*`, `src/lib/notifications/*`, `src/lib/observability/*`, `src/lib/retention.ts`, `src/lib/secrets.ts`, `src/lib/tenancy.ts`, `src/lib/db.ts`, `src/lib/db/constraints.ts`, `src/lib/documents/storage.ts`, `src/lib/integrations/{sync,salesforce,sap,file,dev,types}.ts`, `src/lib/integrations/core/*`, `salesforce/*`, `sap/adapter.ts`, `gpo/{reconcile,api,mock}.ts`, `fx/*`, `documents/service.ts`, `competitor-contracts/mock.ts`; `scripts/{worker,with-secrets,db-preflight,model-eval,gen-constraints,test-adversarial,test-enterprise,lib/harness}.ts`, `prisma/seed.ts`, `prisma/seed-enterprise.ts` (company/bin parts), `prisma.config.ts`, `src/instrumentation.ts`; read-only: `src/app/api/{health,metrics,system,observability/export,analytics/[report]}/route.ts`, `src/lib/analytics/*`, `src/lib/approvals/{service,rules}.ts`, `src/lib/log.ts`, `src/lib/api.ts`; pg-boss 12 internals (`plans.js`, `boss.js`, `manager.js`, `index.js`) for the heartbeat/stop/supervise semantics; `mac-demo` branch via `git show` only.

Behaviours exercised (mandatory items A–V): all, as recorded in §3. Not done / not reachable here: Docker image build and boot (Docker unavailable → BLOCKED, static review + role exercise instead); `npm run build` (WS3); HTTP-level tests of routes (WS4) — the health, metrics, analytics and integration admin routes were called in-process; the multi-process rate limiter was documented, not built (per brief); the mac-demo scripts were read, not executed (no macOS).

## 2. Findings

| id | sev | type | title | reproduction | root cause | fix (files) | regression test | pre-fix evidence | post-fix | status |
|---|---|---|---|---|---|---|---|---|---|---|
| WS5-F01 | P2 | DEFECT | KN-19: `test:adversarial` / `test:enterprise` never exit with inline workers (the default) | `unset JOBS_WORKER; timeout 120 npm run test:adversarial` → all 24 pass, process killed at 120 s (`EV/adversarial-inline-before.txt` exit=124; `EV/enterprise-inline-before.txt` exit=124); with `JOBS_WORKER=off` both exit 0 in 3–8 s | any step that enqueues a job starts pg-boss (own pool + timers) and nothing stops it; `prisma.$disconnect()` alone does not release the loop | `scripts/lib/harness.ts` `releaseResources()` (stops the boss if started, disconnects Prisma; lazy imports so the pure suites never load the DB modules); called at the end of `scripts/test-adversarial.ts` and `scripts/test-enterprise.ts` (2 lines each — WS1 has finished; not an expectation change). Exit codes unchanged: 0 on success, 1 on any failure, no unconditional `process.exit` | `EV/scripts/exit-probe.ts` (boss up → stop → exits in 1.8 s; without stop killed at 20 s) | above | `EV/adversarial-inline-after.txt` 24/24 exit 0 in 4.1 s; `EV/enterprise-inline-after.txt` 18/18 exit 0 in 8.3 s; `EV/adversarial-off-after.txt` 24/24; failing run (bad DB) exits 1 | VERIFIED_FIXED |
| WS5-F02 | P2 | DEFECT | KN-15: legacy `SF_*` / `SAP_*` variables selected API "skeletons" that only threw `NotConfigured("… custom quote objects agreed with the SF admin")` — untruthful once the Tier 2 providers exist; `feedSource("crm")` only knew the legacy env var, so the crm/erp/gpo feeds reported "no source configured" even with the Tier 2 integration enabled | `tests/db/ws5-platform.test.ts` "the crm feed's source is the enabled Tier 2 integration …" (pre-fix: `feedSource("crm").kind === "none"` with Salesforce enabled; `crmAdapter().system === "salesforce"` skeleton with `SF_CLIENT_ID` set) | two generations of adapters wired in parallel; `feedSource` predates Tier 2 | deleted `src/lib/integrations/salesforce.ts`, `sap.ts`; `sync.ts`: `crmAdapter()`/`erpAdapter()` = file → dev only, `legacyEnvPresent()` + `refuseLegacyEnv()` throw a `NotConfigured` naming the variables and Settings → Integrations (crm/erp syncs and `pushQuote`), `integrationStatus` says the variables are ignored; `feeds/index.ts` `feedSource` (now async) reads the enabled Tier 2 rows (salesforce / sap / any gpo:*) → `kind: "api"` with the provider label, no hash; docs (`INTEGRATIONS.md`, `INTEGRATION_SETUP.md` § Environment, `.env.example` route 2 retired, `README.md`) | same test; `tests/db/tier2.test.ts` "integrity" and `scripts/test-enterprise.ts` `crmAdapter().system === "file"` still pass | `EV/ws5-platform-prefix.txt` | 25/25 | VERIFIED_FIXED |
| WS5-F03 | P2 | DEFECT | Feed concurrency: "one ingestion per feed at a time" was a find-then-create — two callers arriving together both ran (schedule + "Sync now", or two workers) | `EV/scripts/feed-race-probe.ts` on a warmed pool: `EV/feed-race-before.txt` **5/5 rounds both ingestions ran** (the cold-pool test passed only by timing) | TOCTOU between `feedRun.findFirst` and `create`; no lock, no unique index | `src/lib/feeds/index.ts` `ingestFeed`: the stale-close, the active check and the RUNNING/SKIPPED row creation run in one transaction under `pg_advisory_xact_lock(hashtext('feed:<name>'))`; a RUNNING row older than 12 h is closed as FAILED ("the process running this ingestion stopped before it finished") instead of blocking the feed for ever | `ws5-platform.test.ts` feeds: "two concurrent ingestions …" and "stale RUNNING rows …" (the latter failed pre-fix: `expected 'RUNNING' to be 'FAILED'`) | `EV/feed-race-before.txt`, `EV/ws5-platform-prefix.txt` | `EV/feed-race-after.txt` 0/5; tests pass | VERIFIED_FIXED |
| WS5-F04 | P2 | DEFECT | `evaluateAlerts().notified` counted, and `lastNotifiedAt` was stamped by, a notification that `notify()` deduplicated (never created) — a condition that resolves and re-fires within the hour restarted the `ALERT_RENOTIFY_HOURS` clock without anyone being told | `ws5-platform.test.ts` "alert lifecycle with a controlled clock …" pre-fix `expected 1 to be +0` | `notifyAlert` returned nothing; the caller assumed a call = a delivery | `notifications/index.ts` `notifyAlert` returns `{ created }`; `observability/alerts.ts` counts and stamps only when `created > 0` | same test (fingerprint dedupe, 5 h 59 / 6 h 01 boundary, failed rule keeps its alert, resolve, re-fire within the dedupe hour) | `EV/ws5-platform-prefix.txt` | pass | VERIFIED_FIXED |
| WS5-F05 | P2 | DEFECT | `/api/health` (and the `queue_stalled` alert) missed a stalled job: the "oldest ready" query only ran when pg-boss's **cached** `readyCount` was > 0; that cache refreshes at most once a minute and less under vacuum back-off | `ws5-platform.test.ts` "/api/health: ready; degraded exactly above the 900 s stalled boundary …" pre-fix `expected 'ready' to be 'degraded'` with a job ready for 901 s | `queueHealth()` gated the live query on a cached counter | `jobs/boss.ts` `queueHealth`: one live grouped query (`min(created_on)` of ready jobs per queue) always; the counts still come from pg-boss; the query is no longer `.catch(() => [])` so a failure surfaces as degraded | same test (899 s → ready, 901 s → degraded, DB down → 503 `down` via `EV/scripts/health-down-probe.ts` in a child process) | `EV/ws5-platform-prefix.txt` | pass | VERIFIED_FIXED |
| WS5-F06 | P2 | DEFECT | Job handlers computed `finalAttempt` from the queue's `retryLimit`, ignoring a per-job override — an exhausted job sent with its own limit was never reported (no `JOB_FAILED`, `onFinalFailure` never ran) | `tests/db/ws5-jobs.test.ts` "a failing job is retried …" pre-fix: timed out waiting for the `JOB_FAILED` notification (`EV/ws5-jobs-prefix.txt`) | `register()` read `QUEUES[name].retryLimit` instead of `job.retryLimit` | `jobs/workers.ts`: `retryLimit = job.retryLimit ?? QUEUES[name].retryLimit`; the final-failure message is passed through `redactMessage` | same test (fails → retried after 1 s → failed, `output` message, `JOB_FAILED` to every ADMIN/PRICING_DIRECTOR, `recentFailures`, `jobs_failed` rule, `boss.retry` puts it back) | `EV/ws5-jobs-prefix.txt` | 8/8 | VERIFIED_FIXED |
| WS5-F07 | P2 | DEFECT | A malformed `FEED_<NAME>_CRON` (or `ALERTS_CRON` / `GUDID_REFRESH_CRON`) threw out of `registerSchedules` and took the whole job system down at start (`jobs.workers_failed`, retried for ever): no pollers for the other queues, no orphan recovery | `ws5-jobs.test.ts` "schedules are UTC crons …": `FEED_PRICING_CRON="every other tuesday"` → `scheduleFeeds(boss)` rejected pre-fix | only embed/analytics/retention used the tolerant `sched` helper; feeds and the two first schedules did not | `feeds/schedule.ts` catches per feed (logs `jobs.schedule_failed`, unschedules that key); `jobs/workers.ts` routes `alerts.evaluate` and `gudid.refresh` through the same helper | same test (bad cron skipped, the others scheduled; `off` unschedules; restore) | `EV/ws5-jobs-prefix.txt` | pass | VERIFIED_FIXED |
| WS5-F08 | P2 | DEFECT | Deployment: `deploy/entrypoint.sh` exec'd `npx next start` / `npx tsx scripts/worker.ts`; `npx` does not forward `SIGTERM`, so `docker stop` ended npx and orphaned the server/worker — no graceful stop, active jobs not handed back | `sh deploy/entrypoint.sh worker` → `kill -TERM <pid>`: exit 143, no `worker.stopping`, `tsx scripts/worker.ts` still running (`EV/entrypoint-roles.txt` first worker block; the orphan was killed by pid) | npx in the exec chain; also `cd /app` made the script unusable outside the image | `deploy/entrypoint.sh`: `cd "${APP_DIR:-$(dirname "$0")/..}"`; exec `node_modules/.bin/next`, `node_modules/.bin/tsx`, `node_modules/.bin/prisma` directly | `EV/entrypoint-roles.txt` "AFTER the fix": `worker.ready` → SIGTERM → `worker.stopping`, exit 0, no orphan; `ws5-jobs.test.ts` external-worker case (SIGTERM → exit 0) | above | pass | VERIFIED_FIXED (image boot itself BLOCKED — no Docker) |
| WS5-F09 | P2 | DEFECT | Sealed integration secrets under a rotated / wrong `INTEGRATIONS_ENCRYPTION_KEY` made **every** save and the settings detail throw ("cannot be decrypted") — the documented recovery ("re-save the secrets under the new key") was impossible | `tests/db/ws5-integrations.test.ts` "sealed secrets: tampering and a wrong key …" (pre-fix `saveConfig` and `integrationDetail` rejected) | `saveConfig`/`secretsPresent` called `open()` unconditionally | `core/config.ts`: unreadable stored secrets are treated as absent for the save — supplied values replace the blob, a save without them keeps the blob untouched and reports the problem (`NOT_CONFIGURED` + error `secrets`); `secretsState()`; `core/admin.ts` detail carries `secretsUnreadable`; key format (32 bytes hex/base64) still enforced; docs `INTEGRATION_SETUP.md` | same test | first run of the file (7 failures incl. this one) | 21/21 | VERIFIED_FIXED |
| WS5-F10 | P2 | DEFECT | Legacy `pushQuote` / `syncCrmAccounts` / `syncErp` decided "Tier 2 enabled?" through `readConfig(...).catch(() => null)` — an enabled Salesforce whose secrets could not be decrypted silently fell back to the **file/dev** adapter (a quote "pushed to CRM" landed in a directory) | code reading; covered by the config test above (readConfig throws) + `sync.ts` `enabled()` now row-based | swallowed error chose the wrong path | `sync.ts` `enabled()` reads the row's flag only; `pushQuote` calls `readConfig` un-caught on the Tier 2 path so the ConfigurationError surfaces | `ws5-platform.test.ts` CRM push gate (file path) + `ws5-integrations.test.ts` config cases | — | pass | VERIFIED_FIXED |
| WS5-F11 | P2 | DEFECT | WS2-F17 / item L: CRM push (`sync.ts pushQuote`, `salesforce/writeback.ts buildQuoteWriteback`) gated on status only — an APPROVED proposal past `validThrough`, or with an included line that lost its price, was still pushed | `ws5-platform.test.ts` "pushQuote refuses an APPROVED proposal past validThrough …" | no `finalizeCheck` on the outbound path | `sync.ts` `assertPushable()` (calls `finalizeCheck`, error `Not pushed to CRM: <reason>`); `writeback.ts` uses `finalizeCheck` after the account-link check (so tier2's "not linked" assertion holds) | same test (expired → refused; unpriced line → refused; fixed → pushed to `outbound/quotes/`) | — | pass; `tier2.test.ts` 16/16 | VERIFIED_FIXED |
| WS5-F12 | P2 | DEFECT | KN-16: `OWN_LABELERS` was documented but not implemented (three hard-coded `["Covidien","Medtronic","Sofradim"]` lists); `seed.ts`'s no-reference-data path `upsert`ed the company **by name** (a second Company row on a database whose company has another name); `seed-enterprise.ts demoData` used `findUniqueOrThrow` by name (throws on drift); the startup tenancy check only warned | `EV/kn16-acme.txt`: fresh DB, `COMPANY_NAME=Acme OWN_LABELERS="Acme Surgical"` → migrate, `seed.ts`, `seed-enterprise.ts` twice, re-seed as Medtronic → **one** Company row `Acme ["Acme Surgical"]`; second row inserted → `checkTenancy` refuses, `scripts/worker.ts` exits 1; `TENANCY_STRICT=false` warns; empty DB → "No company yet" | see title | `src/lib/tenancy.ts`: `defaultCompanyName()`, `defaultLabelers()` (OWN_LABELERS, deduped), `tenancyStrict()` (default true), `checkTenancy({ strict })` throws "Refusing to start: N Company rows …"; `prisma/seed.ts` `resolveCompany()` (both paths); `prisma/seed-enterprise.ts` `resolveCompany()` (both call sites); `scripts/worker.ts` calls `checkTenancy()` before starting; `.env.example`, `DEPLOYMENT.md` | `ws5-platform.test.ts` tenancy; `ws5-config.test.ts` defaults | `EV/kn16-acme.txt` | pass | VERIFIED_FIXED for seeds + worker; **web start-up refusal needs a 2-line patch in `src/instrumentation.ts` (shared, not edited)** — see §5; `src/lib/settings.ts getCompany()` create path still hard-codes the labelers (shared file) — patch in §5 |
| WS5-F13 | P2 | DEFECT | KN-13: FX look-back default 5 (service) vs 3 (registry) and the configured policy was never read — `rateFor` had no callers outside tests, so Settings → FX "fallback" / "max look-back days" did nothing | `grep rateFor\|convertWithProvider src` → only the service; registry default 3 | two definitions, no plumbing | canonical policy in `fx/service.ts` (`FX_MAX_LOOKBACK_DEFAULT = 5`, cap 30, code comment with the holiday-weekend rationale), `fxPolicyFrom(config)`, `configuredFxPolicy()`; registry field default 5 with a 0–30 validator; `INTEGRATION_SETUP.md` § 2.5; the provider-fetched note now also says "(N days earlier)" | `ws5-integrations.test.ts` "FX look-back policy (KN-13)" ×2: default/override/clamp/validation, weekend, holiday Monday (3 days), too-short window, unknown pair, immutable stored rate + `fx.rate_restated_ignored` log, stored-wins | first run of the file | pass | VERIFIED_FIXED (wiring `configuredFxPolicy()` into a conversion caller is left to the callers that will convert — none exist today; documented) |
| WS5-F14 | P3 | DEFECT | SAP adapter: `Number(priceUnit) \|\| 1` read a price unit of 0 or "abc" as 1 (cost misstated silently); a billing quantity ≤ 0 stored the item total as a unit price; one bad row failed the whole page (`MappingError` thrown from `pull`) | code reading | see title | `sap/adapter.ts`: per-row `rejected` (runner → row error → PARTIAL) instead of failing the page; price unit must be a positive number, quantity positive, decimal division (`money().div`) | `tier2-units` SAP cases + `ws5-integrations` fault matrix (partial → PARTIAL with `errored > 0`) | — | pass | VERIFIED_FIXED |
| WS5-F15 | P3 | DEFECT | CW-DBG-0004: `db-preflight` printed 68 `prisma:error` blocks on an empty database | `EV/preflight-empty-before.txt` (68 blocks, exit 0) | each query failed on a missing table and Prisma logged every failure | `scripts/db-preflight.ts` checks `information_schema.tables` once and skips absent tables (one summary line); a missing column still skips | `EV/preflight-empty-after.txt` (one `skip 68 constraint(s)` line, exit 0); `EV/preflight-violation.txt` (2 FAIL rows, exit 1) | above | pass | VERIFIED_FIXED |
| WS5-F16 | P3 | DEFECT | CW-DBG-0005: `model-eval` printed a 12-line stack trace when `OPENAI_API_KEY` was missing | `EV/model-eval-nokey-before.txt` | `main().catch(console.error)` | `scripts/model-eval.ts`: configuration errors are one line (`Model eval: OPENAI_API_KEY is not set …`), exit 1 | `EV/model-eval-nokey-after.txt` | above | pass | VERIFIED_FIXED |
| WS5-F17 | P3 | DEFECT | `documents/storage.ts readDocumentBytes("a/b")` returned `null` (the invalid-id error was swallowed by the not-found catch) | `ws5-integrations.test.ts` documents case pre-fix `promise resolved "null" instead of rejecting` | `fileFor` inside the try | validate the id before the try; `storeDocumentBytes` already refused | same test (also `../etc/passwd`, round trip, unknown id → null) | first run of the file | pass | VERIFIED_FIXED |
| WS5-F18 | P3 | DEFECT | Stored sync errors could carry a credential quoted by a provider: `writeBackQuote` wrote `err.message` raw to `SyncLog`, the legacy `sync.ts log()` wrote `publicErrorMessage(e)` raw; the NDJSON export returned rows verbatim | `ws5-platform.test.ts` "export redaction …" (write side asserted: a fake adapter throwing `token: SENTINEL` → stored error contains `[redacted]`) | no redaction at the write side; none at the export | `salesforce/writeback.ts` and `sync.ts` redact before storing; new `src/lib/observability/redact.ts` `redactRecord()` for the export route (WS4 route patch in §5); `jobs/workers.ts` redacts the JOB_FAILED body | same test | — | pass | VERIFIED_FIXED (write side) / FIXED_AWAITING_VERIFICATION (export route: WS4 to apply `rows.map(redactRecord)`) |
| WS5-F19 | P3 | DEFECT | `retention.ts` swallowed a failed audit write (`.catch(() => undefined)`) — a sweep could delete with no record | code reading | best-effort audit on a compliance-relevant action | `retention.ts`: log `retention.audit_failed` and rethrow (the job fails → `JOB_FAILED`) | retention cases still pass; audit asserted in "dry run mutates nothing …" | — | pass | VERIFIED_FIXED |
| WS5-F20 | P3 | DEFECT | Config parsing: `JOBS_WORKER=on` was "enabled" for enqueuing but never started workers (queue fills silently); `JOBS_POOL_MAX` / `DATABASE_POOL_MAX` / `JOBS_MAINTENANCE_SECONDS` / batch sizes became `NaN` on a typo; an unknown `DATABASE_ADAPTER` silently meant `pg` | `tests/unit/ws5-config.test.ts` | `Number(x ?? d)` everywhere, no enum check | new `src/lib/env.ts` (`intEnv` bounded, `boolEnv` true/false only, `enumEnv`, one `env.invalid` log per bad value); `jobs/boss.ts` `jobsMode()` (inline/external/off; anything else logged and read as inline), pool/maintenance bounds; `src/lib/db.ts` `poolMax()` 1–200, `adapterKind()` refuses unknown values; `workers.ts` batch sizes; docs `OPERATIONS.md`, `DEPLOYMENT.md`, `.env.example` | `ws5-config.test.ts` (13 cases) | — | 13/13 | VERIFIED_FIXED (`src/instrumentation.ts` still reads `process.env.JOBS_WORKER ?? "inline"` directly — patch in §5) |
| WS5-F21 | P3 | SPEC_AMBIGUITY | `runs_failing` threshold: BUILD_NOTES §19.4 says "≥ `ALERT_RUN_FAILURES`", the code and `OPERATIONS.md` say "> `ALERT_RUN_FAILURES`" (fires at 3 with the default 2) | `ws5-platform.test.ts` "runs rule …" pins the code's `>` (2 → no, 3 → yes, 25 h old ignored) | docs disagree | none (kept the code's and OPERATIONS.md's `>`; BUILD_NOTES is shared — one-word patch in §5) | same | — | — | NOT_A_DEFECT_WITH_RATIONALE (behaviour consistent with the ops doc and the rule's own header) |
| WS5-F22 | P3 | DEFECT (docs) | KN-20/21/22/23/24 and BACKUPS: `package.json` 0.4.0 vs FEATURES v0.7; 5 stale schema comments (+3 columns with none); CONTRIBUTING's CI paragraph missing `test:adversarial`, `npm test`, `eval:gate`, the image job; `.env.example` missing 20 variables; BACKUPS.md claimed "nothing else lives on disk" while `DOCUMENT_STORAGE_DIR` holds uploaded document bytes | `EV/schema-comments-before.txt` (5 mismatches, 4 missing); `EV/env-vars-used.txt` vs `.env.example` | drift | `package.json` + lockfile 0.7.0; `CONTRIBUTING.md` (exact CI job lists, "Versioning" paragraph — no tags created); `prisma/schema.prisma` comments only (`gen-constraints` byte-identical: `EV/gen-constraints-sha-before.txt`, `prisma validate` ok); `.env.example` complete (every `process.env`/helper-read name present; container-only block added); `docs/BACKUPS.md` (documents gap, tar step, client-version note, drill row); `docs/DEPLOYMENT.md` topology + process-local state (KN-17); `docs/OPERATIONS.md` | `EV/schema-comments-after.txt` | — | — | VERIFIED_FIXED |
| WS5-F23 | P3 | COVERAGE_GAP → covered | Queue lifecycle, integrations fault matrix, config, health/metrics, retention, analytics snapshots, notifications had no WS-level tests for the boundaries in the brief | — | — | new `tests/db/ws5-jobs.test.ts` (8), `tests/db/ws5-integrations.test.ts` (21), `tests/db/ws5-platform.test.ts` (25), `tests/unit/ws5-config.test.ts` (13), `tests/unit/ws5-salesforce.test.ts` (2) | — | — | 69 cases pass | VERIFIED_FIXED |
| WS5-F24 | P3 | NOT_A_DEFECT | Items verified fine (a row each, per brief): **KN-17** process-local state classified and documented (security/correctness: rate limiter, enrich job; caches: openFDA memo/bucket, metrics counters) — supported topology one web + one worker; **KN-21** constraints right, comments fixed; **KN-24** mac-demo read-only: `reset.sh` refuses non-localhost by substring match on `DATABASE_URL` (`*localhost*\|*127.0.0.1*` — weak: a remote URL containing "localhost" in its path/query would pass; suggest matching the host), `setup.sh` never overwrites `.env`, `seed-demo.ts` is create-only (`update: {}`, existing approved crosses kept, only null list price/COGS filled), `start.sh` moves to `PORT+1` once when busy (one hop only); platform execution unverified; **migrations** full chain on an empty DB clean (15 applied, 68 CHECKs, pgvector); pre-upgrade fixture: preflight fails clearly (2 rows) and changes nothing, `migrate deploy` fails at the constraint migration and changes nothing (Postgres migrations are not transactional in Prisma, but the generated SQL is idempotent — `DROP CONSTRAINT IF EXISTS`), documented recovery `migrate resolve --rolled-back` + deploy verified (`EV/preflight-violation.txt`); `prisma.config.ts` Neon `-pooler` derivation unit-tested (6 URLs); **neon-http**: the adapter rejects `startTransaction` ("Transactions are not supported in HTTP mode") so every `$transaction` site (11) fails before writing — unit-tested; **secrets**: env/file/vault(v1,v2,token file,namespace)/doppler/aws-without-SDK, `SECRETS_OVERRIDE` only "true", redaction of the `secrets.loaded` line, production matrix — the 3-attempt retry lives in `src/instrumentation.ts` (not tested here: shared file); **Dockerfile** static review: non-root `node`, tini, `prisma generate` in the build stage, assets copied, build-time dummy `DATABASE_URL` is an `ENV` of the `build` stage only (runtime is `FROM base`) so it does not leak; `.dockerignore` excludes `.env*`, docs, tests; observations: `data/fixtures` (dev fixtures) is shipped in the image; compose sets `MIGRATE_ON_START=false` on the worker, which never migrates anyway (harmless); **backups**: round trip identical (`EV/backup-roundtrip.txt`); **GPO closeMissing**: a failed middle page closes nothing, a complete roster closes the stale row, a `maxPages` run never closes, an endless next-link stops at 10,000 pages (documented cap; the watermark then advances — see §5); **late job vs DISABLED**: status stays DISABLED, cursor written only after a complete run, stale RUNNING sync jobs cancelled at start; **account resolution** order and conflicts; **documents** review conditions / verify idempotency / storage ids; **mapping engine** no code execution (`__proto__`, `constructor.prototype`, unknown transforms inert), date/int/split/bool transforms; **notifications** preferences/dedupe/idempotent delivery/partial channel/inactive user/admin recipients; **retention** all listed properties; **analytics** fixture → win/loss, bands, conversion, acceptance; throttle; latest; 24 kept; redaction; 403 for SALES_REP; no NaN; **Salesforce** chunked upsert (200/call, allOrNone:false), partial line failure → VALIDATION with count, idempotent retry keys | — | — | — | — | — | — | NOT_A_DEFECT_WITH_RATIONALE |

## 3. Commands (chronological; exit codes; counts)

| # | command | exit | result / evidence |
|---|---|---|---|
| 1 | `psql … DROP/CREATE DATABASE crosswalk_ws5 TEMPLATE crosswalk_dbg; npx tsx prisma/seed-enterprise.ts` | 0 | `EV/seed-enterprise-initial.txt` |
| 2 | `JOBS_WORKER=off npm run test:adversarial` / `test:enterprise` | 0 / 0 | 24/24 in 3.2 s; 18/18 in 8.1 s (`EV/adversarial-off-before.txt`, `EV/enterprise-off-before.txt`) |
| 3 | `unset JOBS_WORKER; timeout 120 npm run test:adversarial` / `test:enterprise` | 124 / 124 | pass then hang (`EV/*-inline-before.txt`) — KN-19 reproduced |
| 4 | `npx tsx EV/scripts/exit-probe.ts --workers [--stop]` | 124 / 0 | boss keeps the loop alive; `stopBoss()` releases it in 1.8 s |
| 5 | after F01: `npm run test:adversarial`, `test:enterprise` (inline), `test:adversarial` (off) | 0 / 0 / 0 | 24/24 4.1 s; 18/18 8.3 s; 24/24 (`EV/*-after.txt`); bad DB → exit 1 |
| 6 | `npx vitest run tests/db/ws5-platform.test.ts` (first, pre-fix) | 1 | 18/20 — feeds stale row, alert count (`EV/ws5-platform-prefix.txt`) |
| 7 | `npx tsx EV/scripts/feed-race-probe.ts` before / after F03 | 0 / 0 | 5/5 both ran → 0/5 (`EV/feed-race-{before,after}.txt`) |
| 8 | `npx vitest run tests/db/ws5-platform.test.ts` (after F03/F04) | 0 | 20/20 |
| 9 | `npx vitest run tests/unit/tier2-units.test.ts tests/db/tier2.test.ts tests/db/tier1.test.ts tests/unit/tier1-units.test.ts` | 1 → 0 | 83/84 (write-back "not linked" ordering) → after moving the link check first: 84/84 |
| 10 | `npx vitest run tests/db/ws5-integrations.test.ts` (first) | 1 | 14/21 (config/FK/FX note/storage/mapping expectations + F09, F17) |
| 11 | same, after fixes | 0 | 21/21 |
| 12 | `npx vitest run tests/db/ws5-jobs.test.ts` (runs 1–3) | 1 / 1 / 0 | 5/8 (test FK order) → 7/8 (F06) → 8/8 in 39 s (`EV/ws5-jobs-prefix.txt`) |
| 13 | `npx tsx EV/scripts/hb-probe.ts` | 0 | supervision claim reset → stale-heartbeat job → `retry` with "job heartbeat timeout" |
| 14 | `DATABASE_URL=…crosswalk_ws5_empty npx tsx scripts/db-preflight.ts` before / after F15 | 0 / 0 | 68 `prisma:error` blocks → 1 skip line (`EV/preflight-empty-{before,after}.txt`) |
| 15 | `npx tsx scripts/model-eval.ts --n 2` (no key) before / after F16 | 1 / 1 | stack trace → one line (`EV/model-eval-nokey-{before,after}.txt`) |
| 16 | `npx prisma migrate deploy` on `crosswalk_ws5_empty` | 0 | 15 migrations, 68 CHECKs, `vector` present, 2.0 s (`EV/migrate-deploy-empty.txt`) |
| 17 | `npx tsx scripts/gen-constraints.ts` + sha256 before/after | 0 | byte-identical (`EV/gen-constraints-sha-before.txt`) |
| 18 | `npx tsx EV/scripts/schema-comments.ts` before / after | 0 | 5 mismatches + 4 uncommented → 0 real (`EV/schema-comments-{before,after}.txt`); `npx prisma validate` ok |
| 19 | pre-upgrade fixture on `crosswalk_ws5_viol` (10 migrations via psql + `migrate resolve --applied`, violating rows, preflight, deploy, recovery) | preflight 1; deploy 1; recovery deploy 0 | `EV/preflight-violation.txt` |
| 20 | `sh deploy/entrypoint.sh check` ×4, `migrate` ×2, `web` ×3 (npx stubbed), passthrough, `worker` (SIGTERM) before / after F08 | as listed | `EV/entrypoint-roles.txt`, `EV/entrypoint-worker{,-after}.log` |
| 21 | `npx vitest run tests/unit/ws5-config.test.ts` | 0 | 13/13 |
| 22 | `npx tsx prisma/seed.ts` on ws5 (WS1 patch) + `npx vitest run tests/unit/match-quality.test.ts` | 0 / 0 | 58 own bins now carry `source:"sku"` evidence; 72/72 (`EV/seed-after.txt`, `EV/match-quality-after-seed.txt`) |
| 23 | KN-16 on `crosswalk_ws5_acme` (migrate, seeds ×2, drift re-seed, tenancy probes, worker refusal) | 0 (worker refusal: 1 as expected) | `EV/kn16-acme.txt` |
| 24 | `npx vitest run tests/db/ws5-platform.test.ts` (health/metrics/redaction added) | 1 → 0 | F05 reproduced → 23/23; analytics added → 25/25 |
| 25 | `npx vitest run tests/db/ws5-integrations.test.ts tests/db/tier1.test.ts tests/db/tier2.test.ts` (after write-side redaction) | 0 | 61/61 |
| 26 | backup drill (`pg_dump` → `crosswalk_ws5_restore`, counts, pgvector) | 0 | `EV/backup-roundtrip.txt` |
| 27 | CI mirror on `crosswalk_ws5_ci`: migrate deploy 0; typecheck 0; check 0 (19); check:enterprise 0 (21); build SKIPPED (WS3); seeds without reference data (cwd trick) 0; `test:adversarial` 0 (24/24); `npx vitest run` 0 (40 files, 686 passed, 1 skipped, 2 m 34 s); `eval:gate` **1** — "BIN_VERSION is 8 but the accepted baseline measured version 6 — re-measure grading on the new bins and accept it; LLM_MODEL is gpt-5.6-astra but the accepted baseline measured gpt-6-astra — measure the new model and accept it" (CW-DBG-0003 prerequisite, expected) | see | `EV/ci-mirror.txt`, `EV/ci-{adversarial,vitest,eval-gate}.txt`; `npm ci` skipped (already installed from the same lockfile) |
| 28 | `npx vitest run tests/unit/ws5-salesforce.test.ts` | 0 | 2/2 |
| 29 | final: `npx vitest run`, `npm run typecheck`, `npm run check`, `npm run check:enterprise`, `npm run test:adversarial`, `npm run test:enterprise` (both with inline workers) on ws5 | see `EV/final-summary.txt` | filled in §3a below |

### 3a. Final verification (`EV/final-summary.txt`)

| command | exit | result |
|---|---|---|
| `npx vitest run` (ws5, full suite incl. every workstream's files present at 09:57) | 0 | 41 files, **688 passed**, 1 skipped, 2 m 40 s (`EV/vitest-full-final.txt`) |
| `npm run typecheck` | 0 | 0 errors (`EV/typecheck-final.txt`) |
| `npm run check` | 0 | 19 passed (`EV/check-final.txt`) |
| `npm run check:enterprise` | 0 | 21 passed (`EV/check-enterprise-final.txt`) |
| `npm run test:adversarial` (inline workers) | 0 | 24 passed, exits by itself in 2.7 s (`EV/adversarial-final.txt`) |
| `npm run test:enterprise` (inline workers) | 0 | **18/18** passed, exits by itself in 6.2 s (`EV/enterprise-final.txt`) |

## 4. Changed files (`git status --short` for my files: `EV/git-status.txt`; diff `EV/ws5-code.diff`)

Modified: `.env.example`, `CONTRIBUTING.md`, `README.md`, `deploy/entrypoint.sh`, `docs/BACKUPS.md`, `docs/DEPLOYMENT.md`, `docs/INTEGRATIONS.md`, `docs/INTEGRATION_SETUP.md`, `docs/OPERATIONS.md`, `package.json` + `package-lock.json` (version only), `prisma/schema.prisma` (comments only), `prisma/seed.ts`, `prisma/seed-enterprise.ts` (company resolution + WS1-F04 bins), `scripts/db-preflight.ts`, `scripts/lib/harness.ts`, `scripts/model-eval.ts`, `scripts/test-adversarial.ts`, `scripts/test-enterprise.ts` (release line only), `scripts/worker.ts`, `src/lib/db.ts`, `src/lib/documents/storage.ts`, `src/lib/feeds/{index,schedule}.ts`, `src/lib/integrations/core/{admin,config,registry}.ts`, `src/lib/integrations/fx/service.ts`, `src/lib/integrations/salesforce/writeback.ts`, `src/lib/integrations/sap/adapter.ts`, `src/lib/integrations/sync.ts`, `src/lib/jobs/{boss,workers}.ts`, `src/lib/notifications/index.ts`, `src/lib/observability/alerts.ts`, `src/lib/retention.ts`, `src/lib/tenancy.ts`.
Deleted: `src/lib/integrations/salesforce.ts`, `src/lib/integrations/sap.ts`.
Added: `src/lib/env.ts`, `src/lib/observability/redact.ts`, `tests/db/ws5-{jobs,integrations,platform}.test.ts`, `tests/unit/ws5-{config,salesforce}.test.ts`, evidence under `EV/`.
Not edited (other owners) — patches in §5: `src/instrumentation.ts`, `src/lib/settings.ts`, `src/app/api/observability/export/route.ts`, `docs/BUILD_NOTES.md`.

## 5. Remaining gaps and what would be needed

1. **`src/instrumentation.ts` (shared)** — two lines: replace `import("@/lib/tenancy").then(({ checkTenancy }) => checkTenancy()).catch(() => undefined)` with an awaited `checkTenancy()` inside the existing try (so a second Company row refuses the web start-up the same way the worker now does; `TENANCY_STRICT=false` keeps the old warn-only behaviour), and `const mode = process.env.JOBS_WORKER ?? "inline"` → `const { jobsMode } = await import("@/lib/jobs/boss"); const mode = jobsMode();`. Until then the web process warns on a second company and treats `JOBS_WORKER=on` as "not inline" (no workers) while `jobsEnabled()` says on.
2. **`src/lib/settings.ts getCompany()` (shared)** — the create-on-miss path: `labelers: JSON.stringify(["Covidien", "Medtronic", "Sofradim"])` → `JSON.stringify(defaultLabelers())` from `@/lib/tenancy` (and `s.companyName` already honours `COMPANY_NAME`). Only reached on a database with no Company row and no seed.
3. **`src/app/api/observability/export/route.ts` (WS4)** — `const body = rows.map((r) => JSON.stringify(redactRecord(r as Record<string, unknown>), …))` with `import { redactRecord } from "@/lib/observability/redact"`; the write side is fixed, this closes the read side for rows written before today. The same helper suits `GET /api/system` `failures` and `/api/integrations/jobs/<id>` row errors.
4. **`docs/BUILD_NOTES.md` (shared)** — §19.4 "≥ `ALERT_RUN_FAILURES`" → "> `ALERT_RUN_FAILURES`"; §18.1 (skeletons removed; legacy env retired), §26.2 KN-13/15/16/17/19 and §26.3 KN-20–24 resolved; §5 add `OWN_LABELERS`, `TENANCY_STRICT`; §4.5 entrypoint execs local binaries; §23.1 add the five `ws5-*` files (69 cases).
5. **Runner pagination cap** — `eachPage` stops at `MAX_PAGES` (10,000) and the since-watermark then advances; a source with more than 10,000 pages (5 M records at 500/page) would skip the tail on the next incremental run. Fix when it matters: return `capped` from `eachPage` and skip `setCursor` (2 lines in `core/runner.ts`); documented here rather than changed because no provider is near it.
6. **Docker** — image build/boot and the `image` CI job are BLOCKED here (`docker info` fails). Next action: on a box with Docker, `docker build -t crosswalk:ci .` then the exact steps of `.github/workflows/ci.yml` `image`; additionally `docker stop web` and confirm the log ends with `jobs.shutdown` (F08).
7. **`eval:gate`** fails as expected until `npm run eval:model -- --accept` is run with a model key at bins v8 / the configured model (CW-DBG-0003, WS1).
8. **mac-demo** — `reset.sh` host check should match the host (`@localhost[:/]`, `@127.0.0.1`) instead of a substring; `seed-demo.ts` hard-codes the labelers (pre-KN-16) — carry `defaultLabelers()` over when `main` is merged; `start.sh` falls back one port only. Not executed (no macOS).
9. Not covered: the distributed rate limiter (documented limitation, per brief), the AWS provider with the SDK installed (module absent), `neon-ws` at runtime, the Salesforce/SAP live APIs beyond the injected-fetch tests, the delegate rule in `notifyApprovalRequested` beyond tier1/tier3 coverage.
10. `tests/unit/tier0-units.test.ts` / `tests/db/ws4-*.test.ts` showed typecheck errors during the run (WS4 in progress); the final `npm run typecheck` result is in §3a.

## 6. Evidence paths

`docs/debug-runs/2026-09-24-full-application/evidence/ws5/`: `seed-enterprise-initial.txt`, `adversarial-{off,inline}-{before,after}.txt`, `enterprise-{off,inline}-{before,after}.txt`, `ws5-platform-prefix.txt`, `ws5-jobs-prefix.txt`, `feed-race-{before,after}.txt`, `preflight-empty-{before,after}.txt`, `preflight-violation.txt`, `model-eval-nokey-{before,after}.txt`, `migrate-deploy-empty.txt`, `gen-constraints-sha-before.txt`, `schema-comments-{before,after}.txt`, `entrypoint-roles.txt`, `entrypoint-worker{,-after}.log`, `seed-after.txt`, `match-quality-after-seed.txt`, `kn16-acme.txt`, `backup-roundtrip.txt`, `env-vars-used.txt`, `ci-mirror.txt`, `ci-{adversarial,vitest,eval-gate}.txt`, `vitest-full-final.txt`, `typecheck-final.txt`, `check-final.txt`, `check-enterprise-final.txt`, `adversarial-final.txt`, `enterprise-final.txt`, `final-summary.txt`, `git-status.txt`, `ws5-code.diff`, `scripts/{exit-probe,feed-race-probe,hb-probe,health-down-probe,schema-comments,tenancy-probe}.ts`.
