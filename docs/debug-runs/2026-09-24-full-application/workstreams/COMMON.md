# Common rules for every workstream agent

Repository: /home/claude/cracr, branch `debug/2026-09-24-full-application` (already checked out; do NOT switch branches, do NOT commit, do NOT push, do NOT `git reset`/`checkout --` other people's files).
Read first: docs/BUILD_NOTES.md (the whole application), AGENTS.md (Next.js 16 notice; docs under node_modules/next/dist/docs), docs/MATCH_QUALITY_MODEL.md and docs/BUSINESS_RULES.md as relevant.

Environment: `set -a; . ./.env.local.wsN; set +a` (N = your workstream number) — this points DATABASE_URL at YOUR OWN disposable local PostgreSQL database `crosswalk_wsN` (seeded copy: reference catalog, 11 dev users, demo commercial data, published crosswalk v1). Never touch Neon, never use `.env`, `.env.local.pg` (the read-only eval DB `crosswalk_ref`) or another workstream's DB. You may reset your own DB freely: `psql "$DATABASE_URL"` for assertions; to rebuild: drop/create `crosswalk_wsN` from template `crosswalk_dbg` (connect to the `postgres` db on the same URL). Run vitest as `npx vitest run <files>`; the full suite as `npx vitest run`. Scripts: `npm run test:adversarial`, `npm run test:enterprise` (note: test:enterprise currently fails 5/18 at baseline because the B12LTH pick changed to NONB12STF — CW-DBG-0002, owned by WS1). No OPENAI_API_KEY exists: model paths must use the in-repo fakes/mocks (`setEmbedderForTests`, mock providers, `setFetchForTests`). openFDA: use recorded fixtures (tests/recorded/openfda) — do not hammer the live API.

Production build for HTTP/browser tests: `npm run build` then `PORT=31NN npx next start -p 31NN` (pick your own port 3101–3105 = workstream number) — remember to kill it (`kill <pid>` by pid, never `pkill -f` with a string that appears in your own command line). Playwright + Chromium are available (`PLAYWRIGHT_BROWSERS_PATH=/opt/pw-browsers`, `executablePath: '/opt/pw-browsers/chromium'` if needed; check `ls /opt/pw-browsers`).

Rules:
- Reproduce before fixing. Every fix needs a regression test that fails for the right reason before the fix and passes after (Vitest; DB tests wrapped in `describe.skipIf(!hasDb)` like the existing suites; put new tests in tests/unit or tests/db under a clearly named file for your workstream, e.g. tests/db/ws2-commercial.test.ts).
- Never weaken authorization, validation, constraints, caps, or evaluation baselines to make something pass. Never run `eval:model --accept`, `prisma migrate reset` against a DB that is not yours, `catalog:hygiene --apply` outside your DB, or anything against Neon.
- Do not print secrets. Do not put customer/reference-sheet content into reports beyond product codes/descriptions needed to describe a defect.
- File ownership (only edit files you own; report other-area defects with a suggested patch in your report instead of editing):
  - WS1 (matching): src/lib/match/**, src/lib/pipeline/**, src/lib/excel/intake.ts, src/lib/cfn.ts, src/lib/gudid/**, src/lib/catalog/**, src/lib/llm/**, src/lib/eval/**, src/lib/learning/**, src/lib/crosswalk/** (governance service), scripts/eval*.ts, scripts/test-enterprise.ts (expectation updates only), prisma/seed-enterprise.ts (demo SKU list only), tests/unit/{match-quality,pacr-regression,openfda-replay,pure-checks}*.test.ts, tests/db/tier3.test.ts (3.5 embedding block only), new tests/**/ws1-*.test.ts, docs/MATCH_QUALITY_MODEL.md.
  - WS2 (commercial): src/lib/{money,contracts,pricing,proposals,approvals,tax,analytics,intelligence,compliance,accounts,outcomes}/** (whatever exists under those names), src/lib/excel/export.ts, src/lib/pdf/**, scripts/check-enterprise.ts, tests/unit/enterprise-checks.test.ts, new tests/**/ws2-*.test.ts, docs/BUSINESS_RULES.md.
  - WS3 (frontend): src/app/**/*.tsx (pages, views, layout — NOT route.ts), src/components/**, src/app/globals.css, public/**, new tests/**/ws3-*.test.ts, Playwright scripts under docs/debug-runs/2026-09-24-full-application/evidence/ws3/.
  - WS4 (security): src/proxy.ts, src/lib/api.ts, src/lib/auth/**, src/lib/security/**, src/lib/redaction* (wherever redactForActor lives), ALL src/app/api/**/route.ts files, tests/unit/tier0-units.test.ts, tests/db/tier0.test.ts, new tests/**/ws4-*.test.ts, docs/DATA_ACCESS_POLICY.md.
  - WS5 (platform): src/lib/integrations/**, src/lib/jobs/**, src/lib/feeds/**, src/lib/notifications/**, src/lib/observability/**, src/lib/db/**, src/lib/retention.ts, src/lib/secrets.ts, src/lib/tenancy.ts, src/lib/env*.ts, src/lib/fx/**, src/lib/documents/**, prisma/schema.prisma (comments only), prisma/migrations (new forward migrations only), scripts/** (except eval*, test-enterprise.ts), deploy/**, Dockerfile, .github/**, .env.example, CONTRIBUTING.md, README.md, package.json (version only), tests/unit/{tier1,tier2}-units.test.ts, tests/db/{tier1,tier2}.test.ts, new tests/**/ws5-*.test.ts, docs/{OPERATIONS,DEPLOYMENT,INTEGRATION*,BACKUPS}.md.
  - Shared files not listed: report, don't edit.
- Your final report (return it as your last message AND write it to docs/debug-runs/2026-09-24-full-application/workstreams/WSN-report.md) must contain: (1) inspected scope (files/behaviours, with what you did NOT get to), (2) findings table — one row per defect: proposed id WSN-F01…, severity P0–P3, type (DEFECT / TEST_DEFECT / DATA_QUALITY / SPEC_AMBIGUITY / COVERAGE_GAP / PREREQUISITE), title, reproduction (exact command/test), root cause, fix (files), regression test name, pre-fix failure evidence, post-fix result, status (VERIFIED_FIXED / FIXED_AWAITING_VERIFICATION / BLOCKED / NOT_A_DEFECT_WITH_RATIONALE / OPEN), (3) every command you ran with exit code and counts, (4) changed files list (`git status --short` for your files), (5) remaining gaps and what would be needed, (6) evidence paths. Keep it factual; no claims without an executed command behind them.

## Server usage (added after WS1 completed)
- WS1 has finished; its changes are in the working tree (BIN_VERSION 8, new tests; full vitest is now 18 files / 388 tests green on a fresh clone of crosswalk_dbg — your wsN DB was cloned BEFORE WS1's fixes, so rebuild your DB from template `crosswalk_dbg` and re-run `npx tsx prisma/seed-enterprise.ts` if you need the NONB12STF demo price; bins cached in your DB are version 7 and will be rebuilt on the next run).
- Only WS3 may run `npm run build` / `npx next start` (port 3103). Only WS4 may run `npx next dev` (port 3104). WS2 and WS5 do not start servers: exercise route handlers in-process from Vitest (see tests/db/tier2.test.ts "admin route authorization" and tests/setup.ts `setActorForTests`) and the proxy via its exported function (tests/unit/tier0-units.test.ts).
